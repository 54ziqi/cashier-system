# 柒号收银系统 — 硬件层架构与融合设计

> 本文档面向研发与运维，完整说明 POS 硬件的设备识别、驱动模型、应用层接入、跨平台桥接、安装包集成方案。

---

## 目录

1. [当前状态诊断](#1-当前状态诊断)
2. [术语定义](#2-术语定义)
3. [硬件驱动层（DEM）设计](#3-硬件驱动层dem设计)
4. [设备识别体系](#4-设备识别体系)
5. [应用层融合方案](#5-应用层融合方案)
6. [跨平台桥接层](#6-跨平台桥接层)
7. [扫描枪数据通道](#7-扫描枪数据通道)
8. [配置与持久化](#8-配置与持久化)
9. [错误处理与容灾](#9-错误处理与容灾)
10. [安装包集成](#10-安装包集成)
11. [测试与验收](#11-测试与验收)
12. [实施阶段清单](#12-实施阶段清单)

---

## 1. 当前状态诊断

| 层 | 模块 | 状态 | 说明 |
|----|------|------|------|
| 驱动 DEM | `drivers/printer.py` | ✅ 完整 | 调用 `escpos.printer` 真实 I/O |
| 驱动 DEM | `drivers/scanner.py` | ✅ 完整 | `pyserial` + `evdev` 双模式 |
| 驱动 DEM | `drivers/scale.py` | ✅ 完整 | `pyserial` + 三种秤协议 |
| 驱动 DEM | `drivers/display.py` | ✅ 完整 | 串口 GBK 编码 + WebSocket 同屏 |
| 驱动抽象 | `base.py` | ✅ 完整 | ABC + DeviceInfo + 回调 |
| 设备注册表 | `registry.py` | ✅ 完整 | 线程安全单例 |
| 应用服务 | `EscPosService.build_receipt` | ⚠️ 假链路 | 手动拼 ESC/POS 字节，绕过 DEM |
| 应用服务 | `EscPosService.process_queue` | ⚠️ 假链路 | 仅标记 done，无真实打印 |
| 设备识别 | L0-L3 识别算法 | ❌ 缺失 | USB/BT 枚举 + 协议探针 |
| 桥接层 | Desktop / Android | ❌ 缺失 | 平台特定 fd 获取通道 |
| 扫码前端 | 数据送达 | ❌ 缺失 | 未连 WS/HID 消费 |
| 安装包 | exe / APK | ❌ 缺失 | 仅 Python 脚本形态 |

**核心问题**：DEM 驱动协议齐全但未接入业务层，业务层有一条独立的"假打印"链路。融合目标是让应用层只走 DEM。

---

## 2. 术语定义

| 缩写/术语 | 含义 |
|-----------|------|
| **DEM** | Driver Extension Module，驱动扩展模块，封装特定硬件的字节协议与 I/O |
| **Bridge** | 桥接层，平台特定的设备枚举与 fd/port 获取（与 DEM 通过 DeviceInfo 解耦） |
| **Registry** | 设备注册表全局单例，运行时维护已连接设备集合 |
| **Protocol Probe** | 协议探针，通过发送标准指令 + 超时判定设备类型 |
| **Behavior Fingerprint** | 行为指纹，监听设备自发数据流判定设备类型 |
| **HID** | Human Interface Device，USB/蓝牙人机接口设备（如键盘式扫码枪） |
| **SPP** | Serial Port Profile，蓝牙串口协议，配对后暴露串口节点 |
| **CDC** | Communication Device Class，USB 通信设备类（USB 串口） |
| **fd** | File Descriptor，Unix 文件描述符，Android Chaquopy 向 Python 传递的 I/O 通道 |
| **VID/PID** | Vendor ID / Product ID，USB 设备标识，用于识别芯片型号 |
| **OUI** | Organizationally Unique Identifier，MAC 地址厂商标识前缀 |

---

## 3. 硬件驱动层（DEM）设计

### 3.1 抽象模型

```
DeviceInfo（配置）  →  BaseDevice（抽象基类）  →  具体驱动实现
                                            ├── ReceiptPrinterDriver
                                            ├── SerialScannerDriver
                                            ├── HIDScannerDriver
                                            ├── SerialScaleDriver
                                            └── CustomerDisplayDriver
```

**`DeviceInfo`** 是所有驱动的统一入口：

```python
@dataclass
class DeviceInfo:
    device_id: str              # 唯一标识，如 "printer_ch340_1a86_7523"
    device_type: DeviceType     # PRINTER / SCANNER / SCALE / DISPLAY / CASH_DRAWER
    name: str                   # 产品友好名，如 "GP-58 Printer"
    connection_type: ConnectionType  # USB_HID / USB_CDC / SERIAL / NETWORK / BLUETOOTH
    config: dict                # 平台特定参数（port/fd/baudrate/host/vid/pid）
    status: str = "offline"     # offline / connected / error
```

### 3.2 驱动接口契约

每个驱动必须实现：

| 方法 | 职责 | 阻塞性 |
|------|------|--------|
| `connect()` | 打开设备、初始化协议、启动后台读循环 | 同步，超时 3s |
| `disconnect()` | 关闭设备、回收资源、终止后台线程 | 同步，立即返回 |
| `is_connected()` | 返回当前连接状态 | 非阻塞 |
| `_notify(data)` | 基类提供，到达数据时调回调 | 非阻塞 |

可选方法（按设备类型）：

| 驱动 | 核心方法 | 触发方式 |
|------|---------|---------|
| 打印机 | `print_receipt(order_dict, shop_config) → bool` | API 调用 |
| 打印机 | `open_cash_drawer() → bool` | API 调用 |
| 扫描仪 | 后台 `_read_loop()`，读到 barcode → `_notify()` | 设备自发 |
| 电子秤 | 后台 `_read_loop()`，解析重量 → `_notify()` | 设备自发 |
| 客显 | `display_total(total, count)`、`clear()` | API 调用 |

### 3.3 回调机制

```python
# API / WebSocket 层注册回调
def on_scanner_data(data: dict):
    """收到扫码枪数据→推送到前端 WS"""
    ws_manager.broadcast(f"/ws/cashier/{tenant_id}", data)

registry.get_scanner().register_callback(on_scanner_data)
```

回调链：**设备读到一个完整帧 → 驱动解析 → `_notify()` → 回调链 → 某处广播到前端**。驱动不关心前端形态（WS/HTTP/浏览器），只负责解析 + 转发。

### 3.4 Feature Flag 分层启用

```python
# config/hardware_profile.py
@dataclass(frozen=True)
class HardwareProfile:
    printer_serial: bool = True
    printer_network: bool = True
    printer_usb: bool = True          # Android 要覆盖为 False
    scanner_serial: bool = True
    scanner_evdev: bool = False       # Android 覆盖为 True
    scale_serial: bool = True
    scale_evdev: bool = False
    display_serial: bool = False      # 默认禁用
    display_ws: bool = False          # 默认禁用
    cash_drawer: bool = False         # 默认禁用
```

驱动入口按 feature 做早期退出：

```python
def create_printer(info: DeviceInfo) -> BaseDevice | None:
    if info.connection_type == ConnectionType.USB and not profile.printer_usb:
        log.warning("USB 打印已按 feature 禁用")
        return None
    return ReceiptPrinterDriver(info)
```

---

## 4. 设备识别体系

### 4.1 四层递进识别模型

```
Layer 0: OS 枚举         — Android UsbManager / Windows WMI / macOS IOKit
Layer 1: 静态字典匹配     — VID/PID / MAC OUI / 设备名称 → 候选类型
Layer 2: 协议探针         — 发 GS I / ENQ / 监听自发数据 → 确认类型
Layer 3: 用户确认         — 候选项 ≥ 2 或全失败 → 弹 UI 选
```

**总时限**：3 秒。每层超时立即跳下一层。95% 设备在 L0 或 L1 命中，5% 进入 L2-L3。

### 4.2 识别算法（跨平台共享）

```python
# app/infra/hardware/detection/probe.py
PORTCONF_TIMEOUT_MS = 300

def probe_as_printer(port_or_fd) -> float:
    """返回置信度 0.0-1.0"""
    try:
        ser = pyserial_like_open(port_or_fd, timeout=PORTCONF_TIMEOUT_MS)
        ser.write(b'\x1d\x49\x41')          # GS I 1（请求打印机 ID）
        ack = ser.read(8)
        if ack and ack[0] == 0x1d:
            return 0.9                        # 高置信：确认 GS I 应答
        ser.write(b'\x10\x04\x01')          # DLE EOT（状态查询）
        ack = ser.read(1)
        if ack and ack[0] & 0x0f != 0x0f:
            return 0.7                        # 中置信：返回有效状态位
    except Exception:
        pass
    return 0.0

def probe_as_scale(port_or_fd) -> tuple[float, str | None]:
    """返回（置信度，秤协议名）"""
    try:
        ser = pyserial_like_open(port_or_fd, timeout=PORTCONF_TIMEOUT_MS)
        deadline = time.time() + 0.5
        frames = []
        while time.time() < deadline:
            chunk = ser.read(40)
            if chunk:
                frames.append(chunk)
            if len(frames) >= 5:
                break
        if not frames:
            # 无自发数据 → 试 ENQ 命令
            ser.write(b'\x05')
            resp = ser.read(16)
            if b'kg' in resp.lower() or b'=' in resp:
                return 0.85, "command_mode"
            return 0.0, None
        # 解析帧长分布判断协议
        lens = [len(f) for f in frames]
        if all(20 <= l <= 24 for l in lens):
            return 0.85, "continuous"
        if all(15 <= l <= 18 for l in lens):
            return 0.8, "kaifeng"
    except Exception:
        pass
    return 0.0, None

def probe_as_scanner(port_or_fd) -> float:
    """扫码枪确认：读 500ms 窗口收到 10-30 位数字字符串即命中"""
    try:
        ser = pyserial_like_open(port_or_fd, timeout=PORTCONF_TIMEOUT_MS)
        deadline = time.time() + 0.5
        buf = b""
        while time.time() < deadline:
            buf += ser.read(64)
            if b"\r" in buf or b"\n" in buf:
                line = buf.split(b"\r\n")[0].decode("utf-8", errors="ignore")
                if line.isdigit() and 6 <= len(line) <= 40:
                    return 0.95
    except Exception:
        pass
    return 0.0
```

### 4.3 识别入口（统一）

```python
# app/infra/hardware/detection/auto_detect.py
def identify_new_device(platform_hint: dict) -> DeviceInfo | None:
    """
    跨平台统一入口。
    platform_hint 由 Bridge 层填充（VID/PID / BT Name / MAC / OS device path）。
    返回识别结果 + 形成的 DeviceInfo，失败返回 None。
    """
    # L1: 静态字典
    match = match_static_table(platform_hint)
    if match.confidence > 0.9:
        return match.result

    # L2: 协议探针
    if platform_hint.get("port") or platform_hint.get("fd"):
        conf, dev_type, protocol = run_protocol_probes(platform_hint)
        if conf > 0.7:
            return build_device_info(dev_type, protocol, platform_hint, confidence=conf)

    # L3: 返回候选列表，由 UI 兜底
    return build_candidate_list(platform_hint)
```

---

## 5. 应用层融合方案

### 5.1 路径选择：API 直调 Registry

选择理由：
- 字节构造是 DEM 职责，API 只传业务数据
- 现有 `EscPosService` 的队列 / 持久化能力保留，仅删除假打代码
- 减少双层抽象带来的代码冗余

架构图：
```
Frontend cashier
    │  POST /print/receipt { order_id: "..." }
    ▼
app/api/merchant/print.py  →  print_receipt()
    │
    ▼
EscPosService.enqueue()            ← 保留：持久化到 SQLite esc_pos_jobs
    │ 同步写入 DB
    │ 立刻派发（不等待后台轮询）
    ▼
DeviceRegistry.get_printer()
    │  ↓ 未连接
    │  返回 {"status": "queued_offline", "job_id": "..."}
    │
    │  ↓ 已连接
    ▼
ReceiptPrinterDriver.print_receipt(order_dict, shop_config)
    │  内部：
    │  1. 拼店铺名 + 订单标题
    │  2. 写 SKU 列表 + 规格
    │  3. 写合计 / 折扣 / 实付
    │  4. 写支付方式 / 会员积分
    │  5. 切纸
    │  6. 开钱箱（若 cash_drawer 启用）
    ▼
返回 {"status": "printing", "job_id": "..."}
    ▼（打印完成）
esc_pos_jobs 表更新 status=done
    ▼（打印失败）
esc_pos_jobs 表更新 status=failed, error_msg="..."
    ▼（下次管理员点 "重打"）
EscPosService.retry_failed(job_id)
```

### 5.2 瘦身后的 EscPosService

```python
class EscPosService:
    def __init__(self, merchant_id: str):
        self.merchant_id = merchant_id

    def enqueue(self, job_type: str, payload: dict) -> str:
        """入队并立即派发，返回 job_id"""
        job = EscPosJobModel.create(self.merchant_id, job_type, payload)
        self._dispatch(job)
        return job.id

    def _dispatch(self, job: EscPosJobModel):
        """根据 job_type 选择驱动"""
        if job.job_type == "receipt":
            printer = registry.get_printer()
            if not printer:
                log.warning("打印机离线，job 保留为 pending")
                return
            ok = printer.print_receipt(job.payload, job.shop_config)
            job.mark_done() if ok else job.mark_failed("print I/O error")

        elif job.job_type in ("label", "kds"):
            printer = registry.get_printer()
            if printer:
                printer.print_label(job.payload)

    def list_jobs(self, status_filter: str = "") -> list[dict]:
        """查询 jobs 列表（不变）"""

    def retry_job(self, job_id: str) -> bool:
        """管理员手动重试失败任务"""

    def process_offline_backlog(self):
        """网络恢复后 / 打印机重连后补打 pending jobs"""
        for job in EscPosJobModel.pending(self.merchant_id):
            self._dispatch(job)
```

### 5.3 新增 API 端点

| 端点 | 方法 | 用途 |
|------|------|------|
| `/api/v1/merchant/devices` | GET | 列出已注册设备 |
| `/api/v1/merchant/devices/scan` | POST | 主动触发 USB/BT 扫描 |
| `/api/v1/merchant/devices/confirm` | POST | 管理员确认注册候选设备 |
| `/api/v1/merchant/devices/{id}` | DELETE | 注销设备 |
| `/api/v1/merchant/devices/{id}/test` | POST | 打印测试页 |
| `/ws/pos/devices` | WebSocket | 实时推送设备状态变化 |

---

## 6. 跨平台桥接层

### 6.1 职责划分

```
app/infra/hardware/
├── drivers/         ← 共享 DEM（一行不动）
├── detection/       ← 共享识别算法（probe.py, auto_detect.py）
├── bridges/
│   ├── __init__.py  ← PlatformBridge ABC
│   ├── desktop.py   ← 桌面端（Win/Mac/Linux）
│   └── android.py   ← Android（Chaquopy 适配）
```

### 6.2 桌面端桥接（`bridges/desktop.py`）

| 系统 | 枚举方式 | 端口获取 |
|------|---------|---------|
| **Windows** | `WMI.PnPEntity` + 注册表 `HKLM\...Enum\USB\VID_xxxx&PID_xxxx` |  FriendlyName 提取 COM 串行 |
| **macOS** | `IOServiceGetMatchingServices(kIOSerialBSDClient)` | IORegistry 映射到 `/dev/cu.*` |
| **Linux** | `pyudev` 枚举 `/dev/ttyUSB*` `/dev/ttyACM*` + `/dev/serial/by-id/*` | 直接拿路径 |

蓝牙 SPP 配对后，三端都由 OS 自动映射为 COM 口 / cu 设备节点。桌面端的 `serial.Serial(port)` 是 pyserial 原生调用，**零桥接代码**。

```python
# bridges/desktop.py
class DesktopBridge(PlatformBridge):
    def scan_usb_serial(self) -> list[PlatformDevice]:
        import serial.tools.list_ports
        candidates = []
        for port in serial.tools.list_ports.comports():
            candidates.append(PlatformDevice(
                os_path=port.device,
                vid=extract_vid(port.hwid),
                pid=extract_pid(port.hwid),
                name=port.description,
                connection=ConnectionType.SERIAL,
            ))
        return candidates

    def scan_bluetooth_spp(self) -> list[PlatformDevice]:
        # 在桌面端，BT SPP 配对结果已被 OS 吸收为串口
        # 名称中含 "Bluetooth" / "BT " / "SPP" 即命中
        import serial.tools.list_ports
        return [p for p in serial.tools.list_ports.comports()
                if any(k in p.description.lower() for k in ("bluetooth", "bt ", "spp"))]

    def open_channel(self, device: PlatformDevice) -> RawIOBase:
        """桌面端：直接 pyserial.Serial"""
        import serial
        return serial.Serial(port=device.os_path, baudrate=9600, timeout=1)
```

### 6.3 Android 桥接（`bridges/android.py`）

Android 无 `/dev/tty*`，由两部分组成：

1. **Java 侧**（`Bridge.java`，< 150 行）：
   - USB: `UsbManager` 枚举 + `usb-serial-for-android` 匹配 CH340/CP2102/FTDI → 读写端点
   - BT SPP: `BluetoothAdapter` 配对 + `RfcommSocket` → InputStream/OutputStream
   - 通过 `ParcelFileDescriptor.createPipe()` 创建管道 → `detachFd()` 拿到 int fd

2. **Python 侧**（`bridges/android.py`，Chaquopy 适配）：
```python
# bridges/android.py
class AndroidBridge(PlatformBridge):
    def scan_usb_serial(self) -> list[PlatformDevice]:
        """调 Java Bridge.scanUsb() 返回候选列表"""
        from java import jarray, jbyte
        bridge = chaquopy.autoclass("com.qh.pos.Bridge")
        raw = bridge.scanUsb()        # 返回 JSONArray
        return [PlatformDevice.from_java(jd) for jd in raw]

    def open_channel(self, device: PlatformDevice) -> RawIOBase:
        """从 Java 拿到 fd → os.fdopen 包装为 Python 文件对象"""
        bridge = chaquopy.autoclass("com.qh.pos.Bridge")
        fd = bridge.attachUsb(device.vid, device.pid)   # int
        import os
        return os.fdopen(fd, "rb+", buffering=0)        # 返回 file-like对象
```

> 关键：Chaquopy 把 Java 对象的 fd (int) 传入 Python，Python 用 `os(fd, "rb+")` 把 int 包装为文件对象。这样 `pyserial.Serial` 的 fd 注入就成立了，DEM 驱动一行不动即可读写。

---

## 7. 扫码枪数据通道

### 7.1 模式 A：HID 键盘式（优选）

**特征**：USB Device Class = 0x03 HID 或 BT Class 0x0504（Keyboard）。

**流程**：
```
扫码枪出码 → Android/Windows/macOS 分发为 KeyEvent → WebView 焦点在 input
    → 10ms 内连续字符输入 + Enter
    → JS 判定为"扫码"而非"手打"
    → ws_manager.emit "barcode_scanned"
    → Frontend 显示 + 加入购物车
```

**判定逻辑（JS）**：

```javascript
// static/js/scanner-hid.js
class HIDScanner {
  constructor(inputEl) {
    this.buf = "";
    this.lastCharTs = 0;
    this.thresholdGapMs = 30;     // 字符间隔阈值
    this.minLength = 6;           // 最小条码长度
    this.input = inputEl;
    this.input.addEventListener("input", (e) => this._onInput(e));
  }
  _onInput(e) {
    const now = Date.now();
    const chars = e.data || "";
    if (now - this.lastCharTs > this.thresholdGapMs && this.buf.length > 0) {
      // 超时，清空过时 buffer
      this.buf = "";
    }
    this.buf += chars;
    this.lastCharTs = now;
    // 检测 Enter 键
    if (this.input.value.endsWith("\n") || this.input.value.endsWith("\r")) {
      const code = this.buf.trim();
      if (code.length >= this.minLength && /^[0-9A-Za-z\-\.\s]+$/.test(code)) {
        this._emit(code);
      }
      this.buf = "";
      this.input.value = "";
    }
  }
  _emit(barcode) {
    window.dispatchEvent(new CustomEvent("qh-barcode-scanned", { detail: { barcode } }));
  }
}
```

### 7.2 模式 B：SPP / 串口式

**前提**：DEM 驱动的 `SerialScannerDriver` 后台 loop 已启动。

**连接流程**：
```
驱动注册回调：
    driver.register_callback(lambda data: ws_broadcast("/ws/pos/cashier", data))

扫码枪出码 → pyserial.read() 接收完整行 → 驱动解析 → _notify() → WS push
```

**WebSocket 协议**：

```json
// 服务 → 前端
{
  "channel": "scanner",
  "event": "barcode",
  "data": { "code": "6901234567890", "ts": "2026-09-13T10:42:33Z" }
}
```

### 7.3 模式判定规则

| 条件 | 使用模式 |
|------|---------|
| USB Class = 0x03 HID or BT Class 0x0504 | HID 键盘（JS 监听） |
| USB Class = 0x0A CDC，或 BT SPP，且有自发数据 | SPP 串口（DEM 驱动） |
| 两者皆可，默认优先 HID | 前端零依赖，兼容性最好 |
| 已确认设备是串口扫码枪但 JS 没反应 | 降级到 DEM 驱动模式 |

---

## 8. 配置与持久化

### 8.1 运行时配置（不入库）

```json
// app/infra/hardware/.runtime/device_bridge.json
{
  "version": 3,
  "known_devices": {
    "printer:ch340:1a86:7523": {
      "device_id": "printer:ch340:1a86:7523",
      "device_type": "printer",
      "name": "GP-58 CH340",
      "connection_type": "serial",
      "config": {
        "port": "/dev/ttyUSB1",
        "baudrate": 19200,
        "protocol": "escpos",
        "auto_reconnect": true
      },
      "stats": {
        "last_connected": "2026-09-13T10:30:00Z",
        "total_prints": 142,
        "failed_prints": 3
      }
    }
  },
  "platform_hint": {
    "last_usb_scan": "2026-09-13T10:42:00Z",
    "last_bt_scan": "2026-09-13T10:42:00Z",
    "unconfirmed_candidates": []
  }
}
```

### 8.2 设备持久化策略

- 设备记录一旦写入 `device_bridge.json`，下次系统启动即加载并 `connect()`，不需要重新识别
- 管理员修改配置：写文件 + registry 热重载
- 设备热插拔：OS 事件触发重新扫描，匹配到已记录 device_id 即直连；发现新弹候选提示

### 8.3 与主数据库关系

`EscPosJob` 入 `app/infra/db/models.py`（已通过 SQLAlchemy 管 SQLite 主库）。设备配置不入主库存，独立的 `.runtime/` 文件夹与业务数据隔离，主打包时也便利排除敏感数据。

---

## 9. 错误处理与容灾

### 9.1 打印机离线

```
API 调 print_receipt
    ↓ printer offline → registry.get_printer() 返回空
    ↓ enqueue 入 SQLite（job.mark_pending）
    ↓ 返回 202 + status: "queued_offline"
    
后台每 30s 调用 EscPosService.process_offline_backlog()
    ↓ 驱动状态变为 is_connected=True → 自动补打
    ↓ 前端 notify.success("离线任务已补打 (3张)")
```

### 9.2 打印失败（缺纸 / 开盖 / 过热）

```
驱动内 escpos 调用 raise IOException → print_receipt() return False
    ↓ job.mark_failed("缺纸")
    ↓ 前端 notify.error("打印机缺纸，请加纸后点击重试")
    ↓ 管理员点「重打」 → retry_job(job_id)
```

### 9.3 扫码枪错误

```
驱动读_loop 收到异常字节 → log.warning(...)
    ↓ 不清空连接，不重启 loop
    ↓ 异常超过 5 次 / min → notify.warn("扫码枪数据异常，建议检查端口")
```

### 9.4 设备拔插

```
OS 驱动断开事件 → Bridge 感知
    ↓ registry.unregister(device_id) 并清理资源
    ↓ ws_manager.broadcast("device_removed", { device_id: "..." })
    ↓ Frontend 图标灰掉
    ↓ 等待 "device_attached" 事件 → 自动走 L0 识别并重连
```

---

## 10. 安装包集成

### 10.1 桌面端（.exe / .dmg / .AppImage）

| 方案 | 首推 | 备选 |
|------|------|------|
| 打 Python + 依赖 | PyInstaller + zipapp 内嵌 | Nuitka 编译为二进制 |
| UI 窗口 | pywebview（用系统 WebView） | Tauri v2（侧载 Python） |
| 体积 | ~45 MB | ~12 MB（Tauri 后续） |
| 硬件驱动 | pyserial + escpos 直接打包进 | 与 pyserial 同 |
| 操作系统权限 | Windows 需用户所属 group 有 COM 口权限 | udev rule for Linux |

### 10.2 手机端（.apk）

| 组件 | 方案 |
|------|------|
| Python 内嵌 | Chaquopy（Android Studio 插件，apk 内嵌 CPython 3.10+） |
| 串口驱动 | chaquopy 打包 pyserial、escpos recipe 完成后可开箱即用 |
| USB 桥 | Bridge.java（usb-serial-for-android） + PyChaBridge.kt（fd 传递） |
| BT SPP 桥 | Kotlin + Android BluetoothAdapter → fd 传递 |
| WebView 窗 | Kotlin setContentView → WebView(Cordova 或 纯 Cordova 式) |
| 硬件权限 | Manifest USB_HOST + BLUETOOTH_CONNECT + 适配 A13+ runtime |

### 10.3 自动更新

```
POS 每 6h 检 GitHub Release latest 版本号
    ↓ 发现新版 → download cashier-v3.x.x.zip → apply_overwrite + restart
    ↓ 资产包覆盖 app/drivers / app/detection / app/bridges
    ↓ DB 结构走 migrations（已有 migrations.py）
    ↓ 热更新需重启服务 → 提醒用户"收银空档点确认重启"
```

---

## 11. 测试与验收

### 11.1 协议层单测

- `test_probe_printer.py`：模拟串口的 BytesIO 注入 GS I 应答 → 置信度 > 0.8
- `test_probe_scale.py`：模拟连续 ASCII 帧 → 正确识别 continuous 协议
- `test_probe_scanner.py`：模拟数字字符串 + \r\n → 置信度 > 0.9
- `test_registry_add_remove.py`：单例多线程安全

### 11.2 驱动层集成测试

- `test_driver_printer_serial.py`：pyserial.Mock + escpos 调用验证/开钱箱/切纸
- `test_driver_scale_read.py`：mock pyserial → 验 read_loop 解析 + tare + 去皮算法
- `test_driver_display_serial.py`：GBK 编码发送验证

### 11.3 端到端测试

| 场景 | 验收标准 |
|------|---------|
| USB 打印机热插拔 | 拔 → 插自动重连，漏打的 job自动补打 |
| USB 键盘扫码枪 | 扫码 → <100ms 内反应在收银台 + 加入购物车 |
| SP 式扫码枪 | 驱动读 loop 起线程 → WS 推送 → 同权效果 |
| 客显串口屏 | API 调 display_total → 串口字清晰钱数 |
| 蓝牙打印机 | 设备配对 → 打一张测试页 → 字迹纸好 |
| 断网 4 小时 | 本地收银未中断，恢复后 backlog 补打 |
| 驱动缺纸 | API 返 503 + 异常标记 + 前端通知 + 一键重试 |

### 11.4 兼容性测试矩阵

| 硬件 | 芯片 | 桌面版 | Android |
|------|------|--------|---------|
| 小票打印机 | 佳博 GP58（CH340） | ✅ | ✅ |
| 小票打印机 | 新北天 NT-58（CP2102） | ✅ | ✅ |
| 小票打印机 | 爱普生 TM-T88（FTDI 内置） | ✅ | ✅ |
| 串口扫码枪 | 新大陆 NLS-EM1300 | ✅ | ✅ |
| USB-HID 扫码枪 | 霍尼韦尔 Vuquest 3320g | ✅ | ✅（浏览器 HID 模式） |
| 蓝牙打印机 | 芝佳 ZJ-5890T（SPP） | ✅（OS 配 COM） | ✅（Java 桥 → fd） |
| 蓝牙电子秤 | 梅特勒 Toledo IND231 | ✅ | ✅ |
| 客显 LED | LED8-N（银河同创） | ✅ | ✅ |

---

## 12. 实施阶段清单

### 第一阶段：应用层瘦身（1-2 周）

- [ ] 删除 `EscPosService.build_receipt()` / `build_label()` / `build_kds()` 三处手动拼字节
- [ ] API `print_receipt` 改为调用 `registry.get_printer().print_receipt()`
- [ ] `process_queue` 改为 `registry.get_printer().print + mark_done/failed`
- [ ] 新增端点 `/api/v1/merchant/devices`（列出已注册设备）
- [ ] 单元测试覆盖 API → DEM 调用路径

### 第二阶段：扫码通道（1 周）

- [ ] `static/js/scanner-hid.js`（浏览器监听 HID 键盘码）
- [ ] 转发 WS 到后端 → 添加到购物车
- [ ] DEM 驱动 `SerialScannerDriver` 实现 WS 推送回调端
- [ ] 验收 HID / 串口两种扫码枪皆通

### 第三阶段：桌面识别层（1-2 周）

- [ ] 新增 `bridges/desktop.py`（Windows WMI / macOS IOKit / Linux udev）
- [ ] 新增 `detection/` 包（probe 算法 + 自动识别入口）
- [ ] 新增端点 `/devices/scan` + `/devices/confirm`
- [ ] 新增 `cashier/devices.html`（设备管理页面）
- [ ] 设备识别 + 弹窗二次确认 → 调 registry.register

### 第四阶段：Android 桥接层（2-3 周）

- [ ] Android Studio 工程初始化 + Chaquopy AAR 引入
- [ ] `android/app/src/main/java/.../bridge/Bridge.java`（< 150 行，USB 枚 + BT 配对 + fd 透传）
- [ ] `bridges/android.py`（Python 侧 Chaquopy 适配）
- [ ] WebView 全屏窗口 → 加载 `http://localhost:{port}/`
- [ ] APK 构建 + 签名 + aab 输出

### 第五阶段：打包与发版（1-2 周）

- [ ] PyInstaller 打 Windows .exe 单文件
- [ ] Briefcase 打 macOS .app
- [ ] chaquopy output aab 通过 Play Store / 厂商应用市场
- [ ] GitHub Release 脚本 + 自动更新检入

---

## 附录 A：关键决策说明

### 选 A 方案（API 直调 Registry）而非 B / C 的理由

- A 保留现有队列/重试基础设施，不用重写
- 删除 `EscPosService.build_receipt` 是最小幅度的消除方案
- 当前 API 层已较薄，直接调 DEM 不嫌过低
- 后期如需复用硬件服务，可在 Registry 面加一层门面，非现在必须

### 选二次确认而非全自动的理由

- 识别不可能 100%，一次错的代价是进出库不准
- 设备收银台频率低（每天最多 5 次），弹一次确认不扰人
- 确认一次后持久化 device_bridge.json → 后续启动全自动不再问

### 选两份 DEM + 分层识别的理由

- 识别算法是无 OS 依赖的纯 Python，Android 上也可运行一次（Chaquopy 调 probe.py）
- 只在"fd 获取"这一步做平台特定（Java 桥 vs pyserial）
- 完整桌面、Android 共享一套协议处理逻辑，修一个 bug 两端受益

---

## 附录 B：Chrome OS 与其他平台

Chrome OS：Chrome OS 104+ 已支持 Android 容器（ARC++），APK 可直接运行；同时 Chrome OS 还有原生 Linux 容器（Crostini），同一份 DEM Python 代码可在 Linux 容器跑。所以 Chrome OS 收银机 = 装 APK + ARC++，DEM 无缝可用。

鸿蒙 HarmonyOS：鸿蒙 NEXT（纯血鸿蒙）当前不支持原生 Python 接 Java 桥。后续如转向鸿蒙，需把 Bridge 代码用重写为 ArkTS，输出 Sidecar DEM 独立二进制，Tauri 级开鸿蒙窗口。当前阶段暂不开展。

嵌入式 Linux（瑞芯微/全志POS 板）：直接跑完整 Python，Linux 下 pyserial + escpos 开箱即用，**桌面 Linux bridge 代码不用改**即能迁移。

---

> 文档版本：v1.0 — 2026-09-13
> 维护者：柒号收银研发团队
> 评审状态：待编码实现
