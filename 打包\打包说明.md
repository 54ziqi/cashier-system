# 柒号收银系统 v3.2 — 打包说明

> 适用版本：3.2.0  
> 维护者：柒号收银研发团队  
> 最后更新：2026-09-13

---

## 目录

1. [目录结构](#1-目录结构)
2. [前置条件](#2-前置条件)
3. [Windows 打包](#3-windows-打包)
4. [macOS 打包](#4-macos-打包)
5. [Android APK 打包](#5-android-apk-打包)
6. [Tauri 备选方案](#6-tauri-备选方案)
7. [Linux AppImage（附赠）](#7-linux-appimage附赠)
8. [常见问题](#8-常见问题)
9. [发布流程](#9-发布流程)

---

## 1. 目录结构

```
收银系统/
└── 打包/                         ← 打包根目录
    ├── 打包说明.md                ← 本文件
    │
    ├── win/                       ← Windows 平台
    │   ├── 构建脚本/
    │   │   ├── cashier.spec      ← PyInstaller 配置文件
    │   │   ├── build.bat          ← 完整构建脚本
    │   │   ├── quick_build.bat    ← 一键构建
    │   │   └── installer.nsi      ← NSIS 安装向导
    │   └── 输出/                  ← 构建产物（exe + 安装包）
    │
    ├── mac/                       ← macOS 平台
    │   ├── 构建脚本/
    │   │   ├── build_dmg.sh       ← DMG 构建主脚本
    │   │   ├── quick_build.sh     ← 一键构建
    │   │   └── Info.plist         ← .app 元信息
    │   └── 输出/                  ← DMG 文件输出
    │
    ├── apk/                       ← Android 平台（完整工程）
    │   ├── build.gradle           ← 项目级 Gradle
    │   ├── settings.gradle
    │   ├── gradle.properties
    │   ├── gradlew                ← Gradle wrapper
    │   ├── app/
    │   │   ├── build.gradle       ← 模块级
    │   │   ├── proguard-rules.pro
    │   │   └── src/main/
    │   │       ├── AndroidManifest.xml
    │   │       ├── java/com/qihhao/pos/
    │   │       │   ├── MainActivity.kt
    │   │       │   ├── Bridge.kt
    │   │       │   ├── UsbReceiver.kt
    │   │       │   └── service/PythonServerService.kt
    │   │       └── res/...
    │   └── 输出/                  ← APK 输出
    │
    ├── 配置模板/                   ← 用户可改的默认配置
        ├── default.toml
        └── lite.toml
```

---

## 2. 前置条件

### 2.1 Windows 打包

| 工具 | 最低版本 | 用途 |
|------|---------|------|
| Python | 3.10+ | 构建入口 |
| pip | 23+ | 依赖安装 |
| PyInstaller | 6.0+ | 将 Python 打包为 .exe |
| NSIS | 3.0+ | 生成安装向导（.exe → 安装包） |
| 7-Zip | 任意 | NSIS 编译必备 |

### 2.2 macOS 打包

| 工具 | 最低版本 | 用途 |
|------|---------|------|
| Python | 3.10+ | 构建入口 |
| pip | 23+ | 依赖安装 |
| PyInstaller | 6.0+ | 将 Python 打包为 .app |
| Xcode Command Line Tools | 任意 | 提供 hdiutil / codesign |
| create-dmg | 任意 | 可选：美化 DMG 布局 |

### 2.3 Android 打包

| 工具 | 最低版本 | 用途 |
|------|---------|------|
| JDK | 17 | Kotlin/Java 编译 |
| Android SDK | API 34 | 目标 SDK |
| Gradle | 8.4 | 构建工具 |
| NDK | 25.x | Chaquopy 编译 Python 依赖 |
| Android Studio | 2023.x | 可选：IDE 工程管理 |

### 2.4 通用依赖（Python 端）

```bash
pip install -r requirements.txt
```

requirements.txt：
```
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.9
pydantic-settings>=2.5
sqlalchemy>=2.0.35
bcrypt>=4.2
cryptography>=43.0
```

---

## 3. Windows 打包

### 3.1 快速构建（推荐）

在 Windows 系统中：

```batch
cd 打包\win\构建脚本
quick_build.bat
```

构建产物将出现在 `打包\win\输出\` 目录中：
- `ZIQI-柒号收银系统-v3.2.0-Setup.exe` — 发行安装包（Windows 安装器）
- `收银系统v3.2\` — 程序目录，包含 PyInstaller 生成的应用文件

> 说明：当前项目的 Windows 分发身份已统一为 `ZIQI`，并且正式交付的是安装包 `.exe`，不是裸可执行程序。

### 3.2 详细构建流程

```batch
REM 第 0 步：安装依赖
pip install -r ..\..\..\requirements.txt
pip install pyinstaller==6.22.3

REM 第 1 步：构建 exe
cd 打包\win\构建脚本
pyinstaller cashier.spec ^
    --distpath ..\..\..\打包\win\输出 ^
    --workpath ..\..\..\打包\win\构建日志\work ^
    --specpath ..\..\..\打包\win ^
    --noconfirm
```

### 3.3 生成安装向导（可选）

在 NSIS 编译环境下的 Windows 机器上：

```batch
cd 打包\win\构建脚本
makensis installer.nsi
```

此操作会生成：
- `打包\win\输出\ZIQI-柒号收银系统-v3.2.0-Setup.exe` — 发行安装包（Windows 安装器）

安装向导功能：
- 选择安装目录（默认 `C:\Program Files\柒号收银`）
- 创建桌面快捷方式
- 注册卸载程序
- 安装完成后自动启动选项
- 使用 `ZIQI` 作为发行签名

### 3.4 构建产物说明

```
打包/win/输出/
├── ZIQI-柒号收银系统-v3.2.0-Setup.exe    ← 正式安装包（NSIS 生成）
├── 收银系统v3.2\                         ← PyInstaller 程序目录
│   ├── 收银系统v3.2.exe                  ← 程序主入口
│   └── config/                           ← 默认配置
└── _internal/                             ← PyInstaller 解压目录（隐藏）
    ├── static/                            ← html/css/js
    ├── config/                            ← default.toml / lite.toml
    └── lib-dynload/              ← Python C 扩展
```

---

## 4. macOS 打包

### 4.1 快速构建

在 macOS 系统中：

```bash
cd 打包/mac/构建脚本
bash quick_build.sh
```

构建产物：`打包/mac/输出/cashier-v3.2.0.dmg`

### 4.2 详细构建流程

```bash
# 前置条件：安装 PyInstaller
pip install pyinstaller

# 执行构建
cd 打包/mac/构建脚本
bash build_dmg.sh
```

### 4.3 DMG 挂载后

挂载 DMG，用户将看到：

```
双击安装
├── 柒号收银.app    → 拖到 Applications 文件夹
└── Applications    → 快捷链接
```

### 4.4 代码签名（App Store 发布必要）

```bash
# 签名 .app 包
codesign --deep --force --verify --verbose \
  --sign "Developer ID Application: Your Name (TEAMID)" \
  "打包/mac/输出/收银系统.app"

# 签名 DMG
codesign --force --sign "Developer ID Application: Your Name (TEAMID)" \
  "打包/mac/输出/cashier-v3.2.0.dmg"

# 验证签名
spctl --assess --type execute "打包/mac/输出/收银系统.app"
```

---

## 5. Android APK 打包

### 5.1 工程结构

```
打包/apk/
├── build.gradle            ← 项目级 Gradle (Chaquopy 15.0.1)
├── settings.gradle
├── gradle.properties
├── gradlew / gradlew.bat   ← Gradle Wrapper
├── app/
│   ├── build.gradle        ← 模块级（Chaquopy + ABI + 依赖）
│   └── src/main/
│       ├── AndroidManifest.xml
│       ├── java/com/qihhao/pos/
│       │   ├── MainActivity.kt        ← WebView 全屏加载
│       │   ├── Bridge.kt              ← Java→Python 桥接
│       │   ├── UsbReceiver.kt         ← USB 热插拔
│       │   └── service/PythonServerService.kt ← 前台服务保活
│       └── res/...                    ← 资源文件
└── local.properties        ← SDK 路径模板（需自行生成）
```

### 5.2 首次构建

```bash
cd 打包/apk

# 第 1 步：生成 local.properties（指向 Android SDK）
echo "sdk.dir=/path/to/your/Android/Sdk" > local.properties

# 第 2 步：通过 Android Studio 打开工程
android-studio .

# 第 3 步：使用 Android Studio Build → Build APK(s)
# 或命令行：
./gradlew assembleDebug     # 调试版
./gradlew assembleRelease   # 发布版（需要签名）
```

### 5.3 APK 签名（发布必备）

```bash
# 生成 keystore（首次执行一次）
keytool -genkey -v -keystore cashier-release.keystore \
  -alias cashier -keyalg RSA -keysize 2048 -validity 10000

# 签名 APK
jarsigner -verbose -sigalg SHA1withRSA -digestalg SHA1 \
  -keystore cashier-release.keystore \
  app/build/outputs/apk/release/app-release.apk cashier

# 对齐优化
zipalign -v 4 app-release.apk cashier-v3.2.0-release.apk
```

### 5.4 构建产物

```
打包/apk/app/build/outputs/apk/
├── debug/app-debug.apk            ← 调试用
└── release/app-release.apk        ← 发布用（签名后: cashier-v3.2.0-release.apk）
```

APK 大小：约 40-60 MB（含 Python 3.12 + FastAPI + SQLAlchemy + Chaquopy）

---

## 6. Tauri 备选方案

### 6.1 适用场景

当对体积（< 15 MB）和性能（内存 < 100 MB）有更高要求时，使用 Tauri v2 替代 PyInstaller。

### 6.2 工程结构

```
打包/win/tauri/  （或独立 tauri-v3.2/）
├── src-tauri/
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   ├── icons/
│   └── src/main.rs
├── src/
│   └── index.html        ← WebView 入口
└── package.json
```

### 6.3 构建流程

```bash
# 安装 Rust + Tauri CLI
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
cargo install tauri-cli

# 构建
cd tauri-project
cargo tauri build
```

---

## 7. Linux AppImage（附赠）

### 7.1 快速构建

在 Linux 系统内：

```bash
pip install python-appimage
python-appimage install python3.10
./python3.10*.AppImage -m PyInstaller 打包/win/构建脚本/cashier.spec
```

---

## 8. 常见问题

**Q1: Windows exe 被 Windows Defender 误报怎么办？**
> A: PyInstaller 出品有时会被启发式检测。解决方案：
> 1. 向 Microsoft 提交误报申请：https://www.microsoft.com/en-us/wdsi/filesubmission
> 2. 使用代码签名证书签名 exe（推荐 Sectigo / DigiCert）
> 3. 购买 EV 证书可立即消除 SmartScreen 警告

**Q2: macOS 提示"无法验证开发者"怎么办？**
> A: 需要使用 Apple Developer Program（$99/年）签名 + 公证（notarize）：
> ```bash
> xcrun notarytool submit cashier.dmg --apple-id your@email.com --password @keychain:AC_PASSWORD --team-id TEAMID --wait
> xcrun stapler staple cashier.dmg
> ```

**Q3: 如何在 Android 上配置 Python 依赖？**
> A: Chaquopy 的 pip 配置在 `app/build.gradle` 内：
> ```groovy
> pip {
>     install "fastapi==0.115.0"
>     install "uvicorn==0.32.0"
>     install "sqlalchemy==2.0.35"
>     install "pydantic==2.9.0"
> }
> ```

**Q4: Android 工程构建失败 "NDK not found"？**
> A: 在 `local.properties` 中追加：
> ```properties
> sdk.dir=/path/to/Android/Sdk
> ndk.dir=/path/to/Android/Sdk/ndk/25.2.9519653
> ```
> 或通过 Android Studio SDK Manager 安装 NDK。

**Q5: Python 版本兼容性？**
> A: 本项目依赖 Python 3.10+。Chaquopy 支持的 Python 版本为 3.8-3.12。确保本地开发版与 APK 内嵌版一致。

**Q6: 如何自定义 exe / app 图标？**
> A:
> - Windows：在 cashier.spec 取消注释 `icon='../../软件源/静态资源/收银.ico'`
> - macOS：替换 `src-tauri/icons/` 下的 `.icns` 文件

**Q7: 如何启用硬件功能（打印机/扫码枪/秤）？**
> A: APP 运行时添加 `--profile default`（默认已开启硬件）或修改 `config/default.toml` 中 `[features] hardware = true`。`--profile lite` 会禁用所有硬件 I/O。

---

## 9. 发布流程

### 9.1 版本号

遵循语义化版本：
- 主版本.次版本.补丁（如 3.2.0 → 3.2.1 修复；3.3.0 新功能；4.0.0 破坏性变更）

### 9.2 发布检查单

**Windows:**
- [ ] PyInstaller 构建成功，exe 可启动
- [ ] 收银全流程跑通（扫码 → 下单 → 打印）
- [ ] NSIS 安装包可安装 + 卸载
- [ ] 测试 Windows 10 / 11 兼容
- [ ] 代码签名（可选但推荐）

**macOS:**
- [ ] .app 打包成功，DMG 可挂载
- [ ] Intel (x64) + Apple Silicon (arm64) 双架构测试
- [ ] 代码签名 + 公证

**Android:**
- [ ] Chaquopy 构建成功
- [ ] 真机测试（商米 / 升腾 / 桑达 / 通用安卓平板）
- [ ] USB Host 扫码枪实测
- [ ] 蓝牙 SPP 打印机实测
- [ ] APK 签名 + 对齐

### 9.3 GitHub Release

```bash
# 打 tag
git tag -a v3.2.0 -m "Release v3.2.0"
git push --tags

# 在 GitHub 创建 Release，上传产物
# - win/ZIQI-柒号收银系统-v3.2.0-Setup.exe
# - mac/cashier-v3.2.0.dmg
# - apk/cashier-v3.2.0-release.apk
# - 打包说明.md
```

### 9.4 自动更新检入（客户端）

安装包首次启动时每 24h 检 GitHub releases/latest：
- 发现新版 → 提示"有新本版 v3.2.1 可用，是否下载？"
- 用户确认 → 下载 zip → 关闭进程 → 解压覆盖 → 重启

---

## 附录：构建环境参考

### Docker 打包环境（推荐）

```dockerfile
FROM python:3.12-windowsservercore  # Windows 容器（仅 Windows 宿主机）
# 或
FROM python:3.12-slim-bookworm      # Linux 容器
RUN pip install pyinstaller
```

### CI / CD (GitHub Actions)

```yaml
# .github/workflows/build.yml
strategy:
  matrix:
    os: [ windows-latest, macos-latest, ubuntu-latest]
steps:
  - uses: actions/checkout@v4
  - uses: actions/setup-python@v5
    with: { python-version: '3.12' }
  - run: pip install -r requirements.txt pyinstaller
  - run: cd 打包/${{ matrix.os == 'windows-latest' && 'win' || matrix.os == 'macos-latest' && 'mac' || 'linux' }}/构建脚本 && ${{ matrix.os == 'windows-latest' && 'quick_build.bat' || 'bash quick_build.sh' }}
```

---

> 文档版本：v1.0  
> 评审状态：待编码实现完成
