# 柒号收银系统 v3.2 — 启动说明与使用指南

---

## 一、系统环境信息

| 项目 | 值 |
|------|-----|
| 操作系统 | Windows 10 (19045) |
| Python 版本 | 3.12.13 |
| 虚拟环境 | `.venv/`（已创建于项目根目录） |
| 配置文件 | `config/lite.toml`（L0 极简模式） |
| 数据库 | SQLite（WAL 模式，自动创建） |
| License | 30 天试用（首次启动自动生成） |

---

## 二、项目包含的服务端

本项目共有 **2 个独立服务**，分别运行在不同端口：

| 服务 | 入口 | 默认端口 | 用途 |
|------|------|----------|------|
| **POS 收银端** | `python run.py serve` | 8000 | 离线收银主服务，管理商品/订单/会员/支付等 |
| **云后台** | `python cloud/run.py` | 9000 | 云端聚合看板，连锁多店数据汇总与可视化 |

---

## 三、POS 收银端（端口 8000）

### 3.1 启动方式

```powershell
# 激活虚拟环境
.venv\Scripts\activate

# 首次运行：初始化数据库 + 创建管理员账户
python run.py init

# 启动服务
python run.py serve
```

### 3.2 首次使用流程

```
1. 运行 python run.py init
   → 自动创建数据库、生成 RSA 密钥对
   → 自动生成 admin 账户，初始密码写入 data/initial-password.txt

2. 运行 python run.py serve
   → 服务启动后自动打开浏览器访问 http://localhost:8000
   → 默认开启 30 天试用 License

3. 使用 initial-password.txt 中的密码登录
   → 首次成功登录后，密码文件自动删除
```

### 3.3 CLI 命令一览

| 命令 | 说明 | 示例 |
|------|------|------|
| `python run.py init` | 初始化数据库和密钥 | `python run.py init` |
| `python run.py serve` | 启动 POS 服务 | `python run.py serve --port 8000` |
| `python run.py serve --no-browser` | 启动服务（不自动开浏览器） | `python run.py serve --no-browser` |
| `python run.py status` | 查看系统状态 | `python run.py status` |
| `python run.py license status` | 查看授权状态 | `python run.py license status` |
| `python run.py license import --file xxx.lic` | 导入授权文件 | `python run.py license import --file license.lic` |
| `python run.py backup` | 备份数据库 | `python run.py backup` |
| `python run.py backup --to D:\backup.zip` | 备份到指定路径 | `python run.py backup --to D:\backup.zip` |

### 3.4 登录认证

**登录接口：** `POST /api/v1/auth/login`

```json
{
  "username": "admin",
  "password": "从 initial-password.txt 获取"
}
```

**返回：**
```json
{
  "token": "xxx|admin|merchant_admin|xxx|hmac_signature",
  "token_type": "local"
}
```

> Token 有效期 8 小时，后续请求在 Header 中携带：`Authorization: Bearer <token>`

### 3.5 API 接口 — 收银员角色

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/auth/login` | 登录 |
| POST | `/api/v1/auth/logout` | 登出 |
| GET | `/api/v1/auth/me` | 当前用户信息 |
| POST | `/api/v1/merchant/cashier/checkout` | 结算结账 |
| GET | `/api/v1/merchant/cart/suspended` | 获取挂单列表 |
| GET | `/api/v1/merchant/members` | 会员列表/按手机号搜索 |
| GET | `/api/v1/merchant/members/{id}` | 会员详情 |
| POST | `/api/v1/merchant/members` | 创建会员 |
| POST | `/api/v1/merchant/members/{id}/recharge` | 会员充值 |

### 3.6 API 接口 — 商品/订单/报表

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/merchant/products` | 商品列表 |
| POST | `/api/v1/merchant/products` | 创建商品 |
| GET | `/api/v1/merchant/products/search` | 扫码搜索商品 |
| GET | `/api/v1/merchant/orders` | 订单列表 |
| GET | `/api/v1/merchant/orders/{id}` | 订单详情 |
| POST | `/api/v1/merchant/orders/{id}/refund` | 申请退款 |
| GET | `/api/v1/merchant/categories` | 分类列表 |
| GET | `/api/v1/merchant/inventory` | 库存数据 |
| GET | `/api/v1/merchant/finance/report` | 财务报表 |
| GET | `/api/v1/merchant/dashboard/today` | 今日销售数据 |
| GET | `/api/v1/merchant/tables` | 桌台管理 |
| GET | `/api/v1/merchant/promotions` | 促销活动 |
| GET | `/api/v1/merchant/policy` | 策略配置 |

### 3.7 API 接口 — 管理员端

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/admin/merchants` | 商户列表 |
| POST | `/api/v1/admin/merchants` | 创建商户 |
| PUT | `/api/v1/admin/merchants/{id}` | 编辑商户 |
| DELETE | `/api/v1/admin/merchants/{id}` | 停用商户 |
| POST | `/api/v1/admin/licenses` | 签发授权许可 |
| GET | `/api/v1/admin/licenses` | 授权列表 |
| GET | `/api/v1/admin/system/stats` | 平台统计数据 |
| PUT | `/api/v1/admin/system/config` | 系统参数配置 |
| GET | `/api/v1/admin/devices` | 设备管理 |

### 3.8 健康检查

| 路径 | 说明 |
|------|------|
| `GET /health/live` | 存活探针 |
| `GET /health/ready` | 就绪探针 |

---

## 四、云后台（端口 9000）

### 4.1 启动方式

```powershell
# 激活虚拟环境
.venv\ Scripts\activate

# 启动云后台服务
python cloud/run.py
# 或指定端口
python cloud/run.py --port 9000
```

### 4.2 访问方式

启动后浏览器访问：**http://localhost:9000/** 进入云端数据看板

### 4.3 API 接口 — 云端聚合

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/cloud/v1/ingest` | 接收 POS 推送的日结签名数据 |
| GET | `/cloud/v1/dashboard/{merchant_id}?days=7` | 单店看板聚合数据 |
| GET | `/cloud/v1/chain/{parent_id}/summary` | 连锁母店看板（汇总所有子店） |
| GET | `/cloud/v1/chain/{parent_id}/stores` | 连锁店列表 |
| GET | `/cloud/v1/tenants` | 已注册商户列表 |
| WS | `/cloud/ws/{parent_id}` | 实时推送 WebSocket |

### 4.4 API 接口 — 策略下发（M6）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/cloud/v1/policies/menu?parent_id=xxx` | 推送菜单策略 |
| POST | `/cloud/v1/policies/prices?parent_id=xxx` | 推送价格策略 |
| POST | `/cloud/v1/policies/members?parent_id=xxx` | 推送会员策略 |
| POST | `/cloud/v1/policies/promotions?parent_id=xxx` | 推送促销策略 |
| GET | `/cloud/v1/policies/{tenant_id}/status` | 查询策略版本 |
| GET | `/cloud/v1/policies/{tenant_id}/pull?since=0` | POS 拉取策略更新 |

### 4.5 看板 7 视图

| # | 视图 | 说明 |
|---|------|------|
| 1 | 今日总览 | 销售额 / 客单 / 订单数 / 同比 |
| 2 | 趋势对比 | 7/30/90 天销售曲线 |
| 3 | 品类分析 | 品类占比环形图、排行柱图 |
| 4 | 时段热力 | 24h × 7d 热力图 |
| 5 | 门店排名 | 多店 GMV 对比（仅连锁母店可见） |
| 6 | 收银员 | 按收银员维度的出勤/效率 |
| 7 | 会员 | 会员消费占比、复购率、积分发放 |

---

## 五、不同角色使用说明

### 5.1 超级管理员（首次使用）

```
步骤：
1. python run.py init → 查看 data/initial-password.txt 获取初始密码
2. python run.py serve → 启动服务
3. 浏览器访问 http://localhost:8000
4. 使用 admin + 初始密码登录
5. 登录后系统自动删除密码文件
6. 进入系统后可：创建商户、签发授权、配置系统参数
```

### 5.2 商户管理员

```
登录路径：http://localhost:8000

可执行操作：
- 管理商品（增删改查、分类管理、库存）
- 管理会员（注册、充值、积分）
- 查看订单、处理退款
- 查看财务报表、今日销售数据
- 管理桌台、促销活动
- 查看收银员效率
- 数据备份/导出
```

### 5.3 收银员

```
登录路径：http://localhost:8000

日常操作流程：
1. 登录系统
2. 扫码或选择商品加入购物车
3. 选择支付方式（现金 / 会员余额 / 微信 / 支付宝）
4. 完成结算结账
5. 支持挂单/取单
6. 可手动搜索商品

注意事项：
- 5 次密码错误锁定 15 分钟
- Token 有效期 8 小时，过期需重新登录
```

### 5.4 连锁母店管理者

```
需额外启动云后台：python cloud/run.py

可执行操作：
- 查看所有门店实时销售数据
- 查看跨店汇总报表
- 向子店下发菜单/价格/会员/促销策略
- 查看所有门店排名
- 查看各维度对比分析

访问方式：http://localhost:9000
认证方式：使用 merchant_id 作为 Bearer Token
```

### 5.5 连锁子店

```
子店独立运行 POS 收银端：
- 数据完全隔离（独立 SQLite 文件）
- 支持离线运行
- 日结数据自动 push 到云后台
- 接收母店下发的策略

License 类型：chain_node（由母店签发）
```

---

## 六、数据存储结构

```
~/.cashier/  或 项目目录/data/
├── data/
│   ├── cashier.db          # POS 端 SQLite 数据库
│   ├── secrets/            # RSA 密钥、Session 密钥
│   ├── logs/               # 运行日志
│   ├── backup/             # 自动备份目录
│   └── initial-password.txt  # 初始密码（首次登录后自动删除）
├── license/
│   └── license.lic         # 授权文件
└── cloud/data/
    └── cloud.db            # 云端聚合数据库
```

---

## 七、License 授权说明

### 授权类型

| 版本 | 年费 | 门店数 | 收银台 |
|------|------|--------|--------|
| 单店标准版 | ¥399/年 | 1 | 3 |
| 连锁旗舰版 | ¥1,599/年 | 5 | ∞ |
| 连锁无限版 | ¥3,999/年 | ∞ | ∞ |

### License 文件结构

```
License 包含：
- license_key: 授权密钥
- type: single | chain_parent | chain_node
- tier: single | chain_flagship | chain_unlimited
- store_name: 强绑定店名（防篡改）
- max_stores / max_cashiers / max_devices
- module_flags: 功能开关
- issued_at / expire_at: 有效期
- hardware_fingerprint: 硬件指纹绑定
- price_tier: 价格方案
- RSA-SHA256 签名
```

### 导入正式授权

```powershell
python run.py license import --file your-license.lic
```

### 受限模式（License 过期后）

| 能力 | 正常模式 | 受限模式 |
|------|----------|----------|
| 新建订单 | ✓ | ✗ |
| 查看历史订单 | ✓ | ✓ |
| 数据导出 | ✓ | ✓ |
| 会员操作 | ✓ | ✗ |
| 打印小票 | ✓ | ✗ |

---

## 八、性能基线

| 指标 | 目标值 |
|------|--------|
| 冷启动时间 | < 2s |
| 空闲内存（L0） | < 80MB |
| 峰值内存（L0） | < 100MB |
| 单笔结账延迟 P95 | < 80ms |
| SQLite 10 万订单 | ≈ 120MB |
| Agent 合计内存 | < 15MB |
| CPU 空闲占用 | < 1% |

---

## 九、常见问题

**Q: 首次启动忘记初始密码怎么办？**
A: 删除 `data/initial-password.txt` 和数据库，重新运行 `python run.py init`

**Q: 端口被占用怎么办？**
A: 配置中 `auto_port = true`，自动向后搜索可用端口。也可手动指定：`python run.py serve --port 8080`

**Q: 连锁母店网断能收银吗？**
A: 能。每子店独立 SQLite + 独立 License，离线自治。

**Q: 如何备份数据？**
A: `python run.py backup` 将数据库和 License 打包为 zip

---

*文档版本: v3.2 · 2026-09-13 · 自动生成*
