# 连锁云后台与授权升级 — 建设方案 v2.0

> 将单店收银系统升级为「单店 + 连锁 + 云后台可视化」三位一体。
> 本方案基于 DDD 现有架构增量扩展，POS 端核心流程不受影响。
> **更新日期**：2026-09-13（v1.0 → v2.0，M1-M5 全部实现完成）

---

## 1. 定价体系（已确定）

| 版本 | 年费 | 门店数 | 收银台 | 适用范围 |
|------|------|--------|--------|---------|
| **单店标准版** | ¥399/年 | 1 | 3 | 夫妻店、档口、摊位 |
| **连锁旗舰版** | ¥1,599/年 | 5 | ∞ | 区域连锁、加盟品牌 |
| **连锁无限版** | ¥3,999/年 | ∞ | ∞ | 大型连锁、零售集团 |

---

## 2. License 升级 ✅

### 2.1 payload 结构

```python
{
  "license_key": "CH-2025-0001-STORE",
  "type": "chain_parent",          # single | chain_parent | chain_node
  "tier": "chain_flagship",        # single | chain_flagship | chain_unlimited
  "merchant_id": "m_xxxxxxxx",
  "store_name": "XX市XX区XX店",     # 强绑定店名
  "parent_merchant_id": "",        # chain_node 时填母店 ID
  "max_stores": 5,
  "max_cashiers": 999,
  "max_devices": 999,
  "module_flags": {"member": true, "report": true, "chain_view": true},
  "issued_at": "2026-09-13T...",
  "expire_at": "2027-09-13T...",
  "hardware_fingerprint": "xxxx",
  "price_tier": {"amount": 1599, "currency": "CNY", "billing": "annual"}
}
```

### 2.2 三类 License

| type | 母店 chain_parent | 子店 chain_node | 单店 single |
|------|-------------------|-----------------|------------|
| 独立 License 文件 | ✅ | ✅ (每子店一张) | ✅ |
| 决定 quota 分配者 | 母店 | — | — |
| 允许离线独立运行 | ✅ | ✅ | ✅ |
| 可创建子店 | ✅ | ❌ | ❌ |
| 可查看跨店报表 | ✅ | ❌ (仅本店) | ❌ |

### 2.3 实现位置

- `app/kernel/license/verifier.py` — RSA 验签 + 硬件指纹校验
- `app/kernel/license/trial.py` — 30 天试用 License 自动生成
- `app/kernel/license/fingerprint.py` — 硬件指纹提取（UUID + CPU + MAC）

---

## 3. 数据隔离（独立 SQLite 文件）

### 实现方式

```
~/.cashier/
├── data/
│   ├── m_local.db                # 单店
│   ├── m_abc123.db               # 连锁母店
│   └── m_abc123.def456.db      # 连锁子店 (merchant_id.node_id)
├── license/
│   ├── license.json              # 母店 License
│   └── nodes/
│       └── def456.json           # 子店 License
├── cloud_export/
│   └── daily_20260913.json       # 日结 push 文件
├── secrets/                      # RSA 密钥对
└── logs/                         # 运行日志
```

每店独立 DB，POS 端启动时按 `merchant_id` 加载对应库。

---

## 4. 店名一致性与防篡改 ✅

- License payload 的 `store_name` 写入 DB `tenants.store_name`
- 前端店名输入框：`disabled`，tooltip = "店名与激活码绑定，如需更改请联系销售"
- 小票/主屏店名从 license 只读渲染

---

## 5. 云后台（独立 FastAPI 进程）✅

### 5.1 部署形态

```
POS 端 (离线)                云后台 (在线)
┌──────────────┐           ┌─────────────────┐
│ FastAPI:8000 │ --push--> │ FastAPI :9000   │
│ SQLite 本地  │   HTTPS   │ SQLite 聚合     │
└──────────────┘           └─────────────────┘
                                  │
                          ┌──────▼──────┐
                          │ 浏览器看板  │
                          │ (7 个视图)  │
                          └─────────────┘
```

### 5.2 云后台实现状态

| 文件 | 行数 | 状态 | 功能 |
|------|------|------|------|
| `cloud/main.py` | 523 | ✅ 已完成 | FastAPI 应用 + 所有端点 + WebSocket + CORS 白名单 |
| `cloud/service.py` | 209 | ✅ 已完成 | 幂等聚合 + 租户注册 + 查询 + 策略追踪 |
| `cloud/models.py` | 97 | ✅ 已完成 | 4 个 ORM 模型（DailyDigest/TenantRegistry/StoreRelation/PolicyPush）|
| `cloud/auth.py` | 77 | ✅ 已完成 | RSA 公钥验签 |
| `cloud/run.py` | — | ✅ 已完成 | CLI 启动入口 |
| `cloud/static/cloud_dashboard.html` | — | ✅ 已完成 | 看板前端 |

### 5.3 POS → Cloud 同步

收银端定时（每日 23:55 或每 4h）把「加密日结文件」push 到云：

```json
{
  "merchant_id": "m_abc123",
  "date": "2026-09-13",
  "gross_sales": 18995.50,
  "order_count": 142,
  "category_breakdown": {"生鲜": 8500, "饮料": 4200},
  "hourly_distribution": [0,0,0,0,0,120,850,2100],
  "signature": "RSA-SHA256(...)"
}
```

**实现位置**：
- `app/infra/sync/daily_export.py` — 日销售聚合 + Top5 品类 + RSA-SHA256 签名
- `app/infra/sync/cloud_push.py` — HTTPS POST + `.pending` 队列 + 重试

### 5.4 云后台 API 完整清单

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| POST | `/cloud/v1/ingest` | 签名校验 | 接收日结 + WebSocket 广播 |
| GET | `/cloud/v1/dashboard/{merchant_id}?days=7` | Bearer Token | 单店看板 |
| GET | `/cloud/v1/chain/{parent_id}/summary` | Bearer Token | 连锁汇总 |
| GET | `/cloud/v1/chain/{parent_id}/stores` | Bearer Token | 连锁店列表 |
| GET | `/cloud/v1/tenants` | Bearer Token | 注册租户列表 |
| WS | `/cloud/ws/{parent_id}` | auth_frame | 实时推送 |
| POST | `/cloud/v1/policies/menu?parent_id=xxx` | Bearer Token | 推送菜单策略 |
| POST | `/cloud/v1/policies/prices?parent_id=xxx` | Bearer Token | 推送价格策略 |
| POST | `/cloud/v1/policies/members?parent_id=xxx` | Bearer Token | 推送会员策略 |
| POST | `/cloud/v1/policies/promotions?parent_id=xxx` | Bearer Token | 推送促销策略 |
| GET | `/cloud/v1/policies/{tenant_id}/status` | Bearer Token | 策略版本查询 |
| GET | `/cloud/v1/policies/{tenant_id}/pull?since=0` | Bearer Token | POS 拉取策略 |
| GET | `/` | 无 | 云看板页面 |

### 5.5 鉴权模型

- **REST 端点**：HTTP Bearer Token = merchant_id，在 `TenantRegistry` 中 lookup
- **母店读子店**：检查 `target.parent_merchant_id == token`
- **WebSocket**：连接后第一帧必须为 `{"token": "xxx"}`，失败 close(4001)
- **降级兼容**：注册表为空时首次放行（首次 ingest 注册后开启校验）

### 5.6 看板 7 视图

| # | 视图 | 数据源 |
|---|------|--------|
| 1 | 今日总览 | 销售额 / 客单 / 订单数 / 同比 |
| 2 | 趋势对比 | 7/30/90 天销售曲线 |
| 3 | 品类分析 | 品类占比环形图 + 排行柱图 |
| 4 | 时段热力 | 24h × 7d 热力图 |
| 5 | 门店排名 | 多店 GMV 对比（仅 chain_parent） |
| 6 | 收银员 | 收银员维度效率 |
| 7 | 会员 | 消费占比 / 复购率 / 积分 |

图表使用 Chart.js（vendored）。

---

## 6. 安全要点 ✅

- 云上报数据 RSA-SHA256 签名，防伪造（P0-01 已修复）
- 云端 License 校验：子店 token 中 `parent_merchant_id` 必须与 chain_parent 匹配
- 店名不可本地修改：前端 disabled + API 只读
- 云端存储最小化：仅存聚合日结，不存会员隐私、不存单笔明细
- POS 离线优先：云上报失败本地队列续传，绝不阻塞收银
- WebSocket 鉴权帧超时关闭（4001）
- CORS 白名单限制（默认 localhost:9000）

---

## 7. 开发里程碑（v2.0 — 全部完成）

### M1 — 授权升级 ✅

- [x] `kernel/license/trial.py` — 试用 License 自动生成
- [x] `kernel/license/verifier.py` — 验签 + 硬件指纹校验
- [x] `kernel/license/fingerprint.py` — 硬件指纹提取
- [x] `kernel/license/payload` 升级（`store_name` / `tier` / `max_stores`）
- [x] DB `tenants` 表新增字段
- [x] API: POST `/admin/licenses`（桩件 → 待接外部授权系统）

### M2 — 连锁子店独立运行 ✅

- [x] `infra/db/engine.py` 按 `merchant_id` 加载不同 SQLite 文件
- [x] 子店激活流程（chain_node License → parent_merchant_id 匹配）
- [x] 跨店数据聚合：`/cloud/v1/chain/{parent_id}/summary`
- [x] `chain/settlement_service.py` — 集团结算
- [x] `chain/transfer_service.py` — 店间调拨

### M3 — 日结同步 + 云后台基础 ✅

- [x] `infra/sync/daily_export.py` — 日结 JSON + RSA 签名
- [x] `infra/sync/cloud_push.py` — HTTPS push + 重试队列
- [x] `cloud/main.py` — 完整 FastAPI 应用
- [x] `cloud/service.py` — 幂等 UPSERT + 聚合
- [x] `cloud/models.py` — 4 个 ORM 模型
- [x] `cloud/auth.py` — 签名验签

### M4 — 可视化看板 ✅

- [x] `cloud/static/cloud_dashboard.html` — 7 视图前端
- [x] Chart.js vendored
- [x] WebSocket 实时推送
- [x] 鉴权：母店可看全部，子店仅自己

### M5 — 策略下发（M6）✅

- [x] `cloud/main.py` — 4 种策略推送端点（menu/prices/members/promotions）
- [x] `cloud/main.py` — 策略拉取端点（`GET /policies/{id}/pull`）
- [x] `chain/policy_client.py` — POS 端拉取 + 应用策略
- [x] 版本追踪和幂等推送

### M6 — 测试审计 ✅

- [x] 196 测试用例全部通过
- [x] 3 轮审计（Reviewer → Hardener → Final Reviewer）
- [x] 75% 变异杀死率
- [x] 全部 P0/P1 问题修复

---

## 8. FAQ

**Q: 连锁母店网断了还能收银吗？**
A: 能。每子店独立 SQLite + 独立 License，离线自治。云看板仅做聚合展示。

**Q: 母店如何防止子店 License 外泄？**
A: chain_node License 绑定硬件指纹 + parent_merchant_id；仅该设备可用。

**Q: 价格会变吗？**
A: 定价字段写在 License payload 里；调价只重发新 License，旧 License 按原价履约到期。

**Q: 云后台性能如何？**
A: 单实例支撑 100+ 门店日结聚合（SQLite），更高并发可换 PostgreSQL。

**Q: 策略下发如何保证可靠性？**
A: 推拉结合：云端推送写入 PolicyPush 表（版本号），POS 端定期 pull 拉取 since 版本后的更新。

---

*文档版本: v2.0 · 2026-09-13 · M1-M6 全部实现完成*
