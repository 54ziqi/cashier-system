# 连锁云后台与授权升级 — 建设方案 v1.0

> 将单店收银系统升级为「单店 + 连锁 + 云后台可视化」三位一体。
> 本方案基于 DDD 现有架构增量扩展，POS 端核心流程不受影响。

---

## 1. 定价体系（已确定）

| 版本 | 年费 | 门店数 | 收银台 | 适用范围 |
|------|------|--------|--------|---------|
| **单店标准版** | ¥399/年 | 1 | 3 | 夫妻店、档口、摊位 |
| **连锁旗舰版** | ¥1,599/年 | 5 | ∞ | 区域连锁、加盟品牌 |
| **连锁无限版** | ¥3,999/年 | ∞ | ∞ | 大型连锁、零售集团 |

---

## 2. License 升级

### 2.1 payload 结构（新）

```python
{
  "license_key": "CH-2025-0001-STORE",
  "type": "chain_parent",          # single | chain_parent | chain_node
  "tier": "chain_flagship",        # single | chain_flagship | chain_unlimited
  "merchant_id": "m_xxxxxxxx",
  "store_name": "XX市XX区XX店",     # ← 强绑定店名，POS/小票显示，不可改
  "parent_merchant_id": "",        # chain_node 时填母店 ID
  "max_stores": 5,                 # single=1, flagship=5, unlimited=999999
  "max_cashiers": 999,
  "max_devices": 999,
  "module_flags": {"member": true, "report": true, "chain_view": true},
  "issued_at": "2026-09-13T...",
  "expire_at": "2027-09-13T...",
  "hardware_fingerprint": "xxxx",
  "price_tier": {
    "amount": 1599,
    "currency": "CNY",
    "billing": "annual"
  }
}
```

### 2.2 三类 License

| type | 母店 chain_parent | 子店 chain_node | 单店 single |
|------|-------------------|-----------------|------------|
| 独立 License 文件 | ✅ | ✅ (每子店一张) | ✅ |
| 决定 quota 分配者 | 母店 | — | — |
| 允许离线独立运行 | ✅ | ✅ | ✅ |
| 可创建子店 | ✅ | ❌ | ❌ |
| 可查看跨店报表 | ✅ | ❌ (仅本店) | ❌ |

---

## 3. 数据隔离（独立 SQLite 文件）

```
~/.cashier/
├── data/
│   ├── m_local.db                # 单店
│   ├── m_abc123.db               # 连锁母店
│   └── m_abc123.def456.db      # 连锁子店 (merchant_id.node_id)
├── license/
│   ├── license.json              # 母店 License
│   └── nodes/
│       └── def456.json           # 子店 License
└── cloud_export/
    └── daily_20260913.json       # 日结 push 文件
```

每店独立 DB，POS 端启动时按 `merchant_id` 加载对应库。

---

## 4. 店名一致性与防篡改

- License payload 里的 `store_name` 写入 DB `tenants.store_name`（来自 license，不是本地输入）。
- 前端「店名」输入框：`disabled`，tooltip = "店名与激活码绑定，如需更改请联系销售"。
- 小票模板渲染：
  ```python
  store_name = app.state.license.payload.get("store_name", "未授权")
  receipt_header = f"{store_name}\n{addr}\n"
  ```
- POS 主屏右上角店名：同上从 license 读取，灰态展示。

---

## 5. 云后台（独立 FastAPI 进程）

### 5.1 部署形态

```
POS 端 (离线)                云后台 (在线)
┌──────────────┐           ┌─────────────────┐
│ FastAPI:8000 │ --push--> │ FastAPI :9000   │
│ SQLite 本地  │   HTTPS   │ PG / SQLite 汇总│
└──────────────┘           └─────────────────┘
                                  │
                          ┌──────▼──────┐
                           │ 浏览器看板  │
                           │ (7 个视图)  │
                           └─────────────┘
```

### 5.2 POS → Cloud 同步

收银端定时 (每日 23:55 或每 4h) 把「加密日结文件」push 到云：

```json
{
  "merchant_id": "m_abc123",
  "date": "2026-09-13",
  "gross_sales": 18995.50,
  "order_count": 142,
  "category_breakdown": {"生鲜": 8500, "饮料": 4200, ...},
  "hourly_distribution": [0,0,0,0,0,120,850,2100,...],
  "signature": "RSA-SHA256(...)"   # 用 license 私钥签名防伪造
}
```

云端只读聚合，不做任何改写。

### 5.3 看板 7 视图

| # | 视图 | 说明 |
|---|------|------|
| 1 | 今日总览 | 销售额 / 客单 / 订单数 / 同比 |
| 2 | 趋势对比 | 7/30/90 天销售曲线，可选同比环比 |
| 3 | 品类分析 | 品类占比环形图、排行柱图 |
| 4 | 时段热力 | 24h × 7d 热力图，识别高峰 |
| 5 | 门店排名 | 多店 GMV 对比（仅 chain_parent 可见） |
| 6 | 收银员 | 按收银员维度的出勤/差错/效率 |
| 7 | 会员 | 会员消费占比、复购率、积分发放 |

图表选用 Chart.js（纯前端，零 npm 依赖）。

---

## 6. 开发里程碑

### M1 — 授权升级（单店 / 连锁 License + 店名防改）
- [ ] `license/trial.py` + `license/payload` 升级，新增 `store_name / tier / max_stores`
- [ ] `kernel/license/verifier.py` 校验新增字段
- [ ] DB `tenants` 表新增 `store_name / parent_merchant_id / license_tier`
- [ ] API: POST `/admin/merchants/{id}/issue-node-license`（母店发子店 License）
- [ ] 前端：POS 主屏显示 `store_name` (disabled input)；小票模板替换

### M2 — 连锁子店独立运行
- [ ] `infra/db/engine.py` 按 `merchant_id` 加载不同 SQLite 文件
- [ ] 子店激活流程：输入 chain_node License → 验证 parent_merchant_id 一致 → 锁定店名
- [ ] 跨店数据聚合接口：`GET /cloud/v1/chain/{parent_id}/summary`

### M3 — 日结同步 + 云后台基础
- [ ] `infra/sync/daily_exporter.py` — 日结 JSON + RSA 签名
- [ ] `infra/sync/cloud_push.py` — HTTPS push 到云
- [ ] 云侧 FastAPI 进程 (`cloud/` 目录)
  - [ ] POST `/cloud/v1/ingest` — 接收签名日结
  - [ ] GET `/cloud/v1/dashboard/{merchant_id}?range=7d`
  - [ ] GET `/cloud/v1/chain/{parent_id}/stores`

### M4 — 可视化看板
- [ ] `cloud/static/dashboard.html`
- [ ] Chart.js (CDN / vendored)
- [ ] 7 视图前端
- [ ] 鉴权：License key → token，链主可查看所有，子店仅自己

### M5 — 测试 + 部署脚本
- [ ] cloud 端 30+ 用例（签名校验/隔离/看板聚合）
- [ ] Docker Compose: pos + cloud 一键拉起

---

## 7. 安全要点

- 云上报数据必须 RSA-SHA256 签名，防伪造。
- 云端 License 校验：子店 token 中 `parent_merchant_id` 必须与 chain_parent 匹配。
- 店名不可本地修改：前端 disabled + API 字段只读。
- 云端存储最小化：仅存聚合日结，不存会员隐私、不存单笔明细。
- POS 离线优先：云上报失败本地队列续传，绝不阻塞收银。

---

## 8. FAQ

**Q: 连锁母店网断了还能收银吗？**
A: 能。每子店独立 SQLite + 独立 License，离线自治。云看板仅做聚合展示。

**Q: 母店如何防止子店 License 外泄？**
A: chain_node License 绑定硬件指纹 + parent_merchant_id；License 文件只能在该台 POS 上用。

**Q: 价格会变吗？**
A: 定价字段写在 License payload 里；调整价格只需重发新 License，旧 License 按原价格履约到期。

---

*文档版本: v1.0 · 2026-09-13 · 与 CatPaw 协作梳理*
