# 行业模板与全业态升级方案 v1.0

> 基于「柒号收银系统 v1.0（单店快餐架构）」升级至 **6 业态全覆盖 / 企业级连锁** 的完整设计方案。
> 本次决策来自与创业者 ZIQI 的深度对齐：全业态、独立 ERP 表、完整 CRM、规则引擎、ESC/POS、一键下发、全离线容灾、三张财务报表。

---

## 0. 现状基线

### 0.1 已有资产（v1.0 已上线）

| 模块 | 状态 |
|---|---|
| 单店离线收银 | ✅ 商品/订单/会员/支付 完整闭环 |
| License RSA-PSS 验签 | ✅ 硬件绑定 + tier 功能开关 |
| Token Version 撤销 | ✅ 替代 revoked_tokens 表 |
| 退款原子性 (SAVEPOINT) | ✅ CAS + 库存归还 + 余额归还 |
| 云聚合看板 (cloud/) | ✅ WS 实时 + 签名入库 + CORS |
| Local 7-view 看板 | ✅ Chart.js SPA |
| 测试 105 cases | ✅ |

### 0.2 能力缺口（本次补全范围）

- 快餐以外的 5 个业态无配置模板
- 订单状态机缺 **制作/上菜/清台** 中间节点
- 商品缺 **规格/套餐/沽清/配方(BOM)**
- 会员缺 **储值/等级/积分规则/消费标签/CRM 推送**
- 促销缺 **规则引擎**
- 供应链缺 **原料/进销存/盘点**
- 财务缺 **三张报表**
- 审计链不完整、离线无容灾、KDS 无厨显、硬件无驱动

---

## 1. 行业模板体系（已落地）

### 1.1 代码位置

| 文件 | 职责 |
|---|---|
| `app/domain/industry/industry.py` | 业态枚举 + 24 功能键 + `IndustryTemplate` 数据类 + 辅助函数 |
| `app/domain/industry/__init__.py` | 模块导出 |
| `app/infra/industry_templates.json` | 前端消费的业态元数据（含 feature_groups 文案） |
| `app/infra/db/models.py` (Tenant.industry 列) | 租户绑定的业态类型 |

### 1.2 功能矩阵（feature × industry）

```
Feature                       快餐  正餐  茶饮  火锅  烘焙  零售
─────────────────────────────────────────────────────────────
table_management               ✗    ✓    ✗    ✓    ✗    ✗
kitchen_display                ✓    ✓    ✓    ✓    ✗    ✗
recipe_bom                     ✓    ✓    ✓    ✓    ✓    ✗
stock_alert                    ✓    ✓    ✓    ✓    ✓    ✓
procurement                    ✓    ✓    ✓    ✓    ✓    ✓
physical_inventory             ✓    ✓    ✓    ✓    ✓    ✓
purchase_suggestion            ✗    ✓    ✗    ✓    ✗    ✓
member_points                  ✓    ✓    ✓    ✓    ✓    ✓
member_stored_value            ✓    ✓    ✓    ✓    ✓    ✓
member_levels                  ✗    ✓    ✓    ✓    ✓    ✓
promotion_discount             ✓    ✓    ✓    ✓    ✓    ✓
promotion_coupon               ✗    ✓    ✓    ✓    ✓    ✓
print_receipt                  ✓    ✓    ✓    ✓    ✓    ✓
print_label                    ✗    ✗    ✓    ✗    ✓    ✓
combo_item                     ✓    ✓    ✓    ✓    ✓    ✓
weight_sale                    ✗    ✗    ✗    ✓    ✓    ✓
weight_price                   ✗    ✗    ✗    ✓    ✓    ✓
time_pricing                   ✗    ✓    ✓    ✓    ✓    ✗
multi_warehouse                ✓    ✓    ✓    ✓    ✓    ✓
supply_chain                   ✓    ✓    ✓    ✓    ✓    ✓
invoice_report                 ✓    ✓    ✓    ✓    ✓    ✓
chain_push                     ✗    ✗    ✗    ✗    ✗    ✗
order_void_all                 ✓    ✓    ✓    ✓    ✓    ✓
audit_log                      ✓    ✓    ✓    ✓    ✓    ✓
offline_grace                  ✗    ✓    ✗    ✓    ✗    ✓
data_backup                    ✓    ✓    ✓    ✓    ✓    ✓
```

> `chain_push` 初始为 ✗，M6 落地后改为对连锁类型启用。
> `offline_grace` 仅正餐/火锅/零售启用，其他业态上云优先。

### 1.3 商品扩展字段（按业态）

| 字段 | 快餐 | 正餐 | 茶饮 | 火锅 | 烘焙 | 零售 |
|---|---|---|---|---|---|---|
| specs (规格组) | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| is_combo | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| bom_json (配方) | ✓ | ✓ | ✓ | ✓ | ✓ | ✗ |
| is_86 (沽清) | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| prep_time (出餐分钟) | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ |
| course_type (上菜顺序) | ✗ | ✓ | ✗ | ✗ | ✗ | ✗ |
| min_order_qty (起售量) | ✗ | ✓ | ✗ | ✗ | ✗ | ✗ |
| toppings / 加料 | ✗ | ✗ | ✓ | ✗ | ✗ | ✗ |
| recipe_ratio | ✗ | ✗ | ✓ | ✗ | ✗ | ✗ |
| product_type (锅底/涮肉) | ✗ | ✗ | ✗ | ✓ | ✗ | ✗ |
| is_weighing | ✗ | ✗ | ✗ | ✓ | ✓ | ✓ |
| batch_no (批号) | ✗ | ✗ | ✗ | ✗ | ✓ | ✓ |
| mfg_date / exp_date | ✗ | ✗ | ✗ | ✗ | ✓ | ✓ |
| discount_at_2h | ✗ | ✗ | ✗ | ✗ | ✓ | ✗ |
| rack_location (货架位) | ✗ | ✗ | ✗ | ✗ | ✗ | ✓ |

> 所有扩展字段以 `JSON` 列 (`Product.specs_json`) + 标准列 (`is_combo`, `is_86`, `bom_json`) 双轨存储，避免 schema 爆炸。规格组通过独立的 `product_spec_groups` / `product_spec_options` 表管理（已落地）。

---

## 2. 六里程碑路线图

### M1 · 订单全生命周期 + 商品规格体系

**目标**：订单状态覆盖堂食全履约流程；商品可挂规格组/做法/加料。

#### 2.1.1 订单状态机（按业态差异化）

```
快餐 (fast_food):
  pending → paid → making → ready(取餐号) → served → checkout

正餐 (full_service):
  reserved → seated → ordering → ordered(压桌)
          → making → fire(等叫/叫起) → served → pre_checkout → checkout → cleared

茶饮 (beverage):
  pending → paid → queued → making → ready(取餐号) → served → checkout

火锅 (hotpot):
  reserved → seated → pot_ordered → pot_served
          → ordering(续点) → making → served → checkout → cleared

烘焙 (bakery):
  pending → paid → checkout (含批号追踪 / 临期标记)

零售 (retail):
  scanning → paid → checkout(含出场通道)
```

> 所有业态共用最终状态：`voided`(整单作废) / `refunding` / `refunded`。

#### 2.1.2 表结构演进

- `orders` 新增列：`kitchen_status` / `table_id` / `table_name` / `course_sequence` / `is_merged` / `spec_text` / `queue_no` / `pot_flavor` / `batch_no` / `is_exp_discount`
- 新增 `tables` 表（desk management）：id / merchant_id / name / capacity / status(seated/empty/reserved) / current_order_id
- 新增 `order_status_logs`：订单状态变更历史（actor / from / to / ts）

#### 2.1.3 API

- `GET /api/v1/merchant/tables` — 桌台列表
- `POST /api/v1/merchant/tables/{id}/seat` — 开台
- `POST /api/v1/merchant/orders/{id}/fire` — 叫起 / 等叫切换
- `POST /api/v1/merchant/orders/{id}/urge` — 催菜
- `POST /api/v1/merchant/orders/{id}/merge` — 联台
- `POST /api/v1/merchant/orders/{id}/void` — 整单作废
- 已有商品 CRUD 升级为支持 `specs_json` / `is_combo` / `bom_json` / `is_86`

#### 2.1.4 前端

- 收银台增加「规格弹窗」：选择杯型/温度/糖度/加料
- 正餐用户可见「桌台地图」入口（M1 先做简化版六宫格）
- 小票 HTML 模板

#### 2.1.5 验收标准

- 105 现有用例仍绿
- +30 新用例：各业态订单状态流转 / 桌台开台 / 规格选择计价 / 整单作废

---

### M2 · 会员 CRM（储值 + 积分 + 等级 + 消费标签 + 营销推送）

#### 2.2.1 新增表

| 表 | 关键字段 |
|---|---|
| `member_wallets` | member_id / balance / total_deposit / total_spent |
| `member_wallet_txns` | wallet_id / type(deposit/consume/refund/reward) / amount / ref_order_id / ts |
| `member_points_txns` | member_id / type(earn/expire/redeem) / points / ref_order_id / ts |
| `member_levels` | id / name / min_spend / min_points / discount_pct / benefits_json |
| `member_level_history` | member_id / old_level / new_level / reason / ts |
| `member_tags` | member_id / tag / source(manual/auto) / ts |
| `member_preferences` | member_id / favorite_products / avoid_ingredients / seat_pref / avg_visit_interval |
| `campaign_tasks` | id / type(birthday/welcome/reactivation) / member_id / status / payload |

#### 2.2.2 业务规则

- **储值**：充值赠送（满 200 送 20 规则从 License 配置读）；消费退单时余额退还。
- **积分**：每消费 1 元得 N 积分；积分 100:1 抵扣；积分有效期 12 月滚动。
- **等级**：年消费额达阈值自动升级；会员价折扣按等级。
- **消费标签**：每次订单落库触发 Event → 后台 Job 定时聚合「到店时段偏好」「辣度偏好」「忌口」存入 `member_tags`。
- **营销推送**：
  - 生日券：生日 T-2 发券（站内 + WebSocket）
  - 沉睡唤醒：30 天未到店 → 自动下发 "欢迎回来" 满减券（站内 + WebSocket）
  - 升/降级通知

#### 2.2.3 API

- `POST /api/v1/merchant/members/{id}/deposit` – 充值（含赠送规则）
- `POST /api/v1/merchant/members/{id}/redeem-points` – 积分兑换
- `GET /api/v1/merchant/members/{id}/wallet` – 余额 + 积分 + 等级
- `GET /api/v1/merchant/members/{id}/tags` – 消费标签
- `GET /api/v1/merchant/campaigns` – 营销任务队列

#### 2.2.4 验收标准

- 储值充值赠送 / 积分消费 / 等级升降
- 生日推送（mock 时钟）/ 沉睡推送（mock 时钟）
- 退单时积分扣减 + 余额退还
- +20 用例

---

### M3 · 促销规则引擎

#### 2.3.1 规则模型

```python
class PromotionRule:
    id, merchant_id, name,
    type(full_reduction / discount / gift / time_special / coupon),
    priority,                        # 数字越小越高
    conditions_json,                 # {min_amount, time_range, categories, members_only}
    actions_json,                    # {discount_amount, discount_pct, gift_product_id, coupon_template_id},
    start_at, end_at,
    stackable(bool),                 # 是否可与其他规则叠加
    status(active/paused/expired),
    created_at

class CouponTemplate:
    id, merchant_id, name, type(amount/percentage),
    value, min_amount, valid_days,
    applicable_products_json,        # 全部/null/指定 IDs
    total_qty, issued_qty

class Coupon:
    id, template_id, member_id,
    code(唯一), status(used/unused/expired),
    issued_at, used_at, ref_order_id
```

#### 2.3.2 执行引擎

```
Order.checkout():
  1. 收集所有 active rules（time within / status = active）
  2. 按 priority 升序排列
  3. 对每条规则：
     a. 校验互斥（若 stackable=False，匹配后立即 break）
     b. 校验 conditions_json（金额/时段/品类/会员限定）
     c. 累加 actions_json 到 discount / gift_items / coupons[]
  4. 计算最终金额 = subtotal - Σdiscount
  5. 创建 coupon 实例（如规则包含券发放）
```

#### 2.3.3 券码核销

- `POST /api/v1/merchant/coupons/redeem` — 扫码 → 查询券码 → 按模板规则应用折扣

#### 2.3.4 验收标准

- 单一规则应用 / 叠加规则独立 / 有限时段规则过期自动失效
- 满减边界值 / 折扣精度 / 整单作废联动回滚
- +25 用例

---

### M4 · 供应链 ERP（独立表方案）

#### 2.4.1 新增表

| 表 | 关键字段 |
|---|---|
| `materials` | id / merchant_id / name / unit / cost_price / stock / safety_stock / status |
| `warehouses` | id / merchant_id / name / type(main/secondary) / status |
| `warehouse_stocks` | warehouse_id / material_id / qty / (uq wh+mat) |
| `recipe_boms` | id / product_id / material_id / qty_wastage / wastage_pct |
| `purchase_orders` | id / merchant_id / supplier / total_amount / status(draft/approved/partially_received/received/void) / created_at |
| `purchase_order_items` | po_id / material_id / qty_ordered / qty_received / unit_price |
| `stock_movements` | id / warehouse_id / material_id / type(inbound/outbound/transfer/adjust) / qty / ref_table / ref_id / ts |
| `physical_inventory_sheets` | id / warehouse_id / status(draft/counted/approved) / total_variance |
| `physical_inventory_lines` | sheet_id / material_id / book_qty / counted_qty / variance |

#### 2.4.2 业务流

```
采购申请 → 审批 → 入库
    ↓
  自动校验安全库存 → 补货建议(加权平均日销 × 备货天数 - 当前库存)
    ↓
  发出采购单 → 到货收货 → stock_movements.inbound
    ↓
  生产领料 / 销售出库 → stock_movements.outbound
    ↓
  月结盘点 → 审批 → 调整单覆盖 warehouse_stocks
```

#### 2.4.3 成本核算

- 加权平均：每次入库后重新计算物料 unit_cost
- 菜品成本 = Σ(BOM 物料 qty × 当前平均成本) / 出品数量
- 毛利 = 销售收入 - 菜品成本 - 物料损耗

#### 2.4.4 API

- `GET /api/v1/merchant/inventory/stock` — 多仓库库存总览
- `POST /api/v1/merchant/purchase-orders` — 采购单
- `POST /api/v1/merchant/inventory/transfer` — 调拨
- `POST /api/v1/merchant/inventory/count` — 盘点
- `GET /api/v1/merchant/inventory/replenishment` — 补货建议

#### 2.4.5 验收标准

- 入库后库存更新 / 销售联动出库 / 盘点差异入账
- 补货建议正确计算
- BOM → 菜品成本 → 毛利
- +30 用例

---

### M5 · 财务报表 + 审计链 + 全离线 + ESC/POS + KDS 厨显

#### 2.5.1 三张财务报表

| 报表 | 数据来源 | 核心指标 |
|---|---|---|
| **利润表** | orders + materials 成本 + 毛利率 | 收入 / 销售成本 / 毛利润 / 费用 / 净利润 |
| **收银对账单** | payments + cash_movements | 各支付方式金额 / 期初/期末现金余额 / 长短款 |
| **员工业绩表** | orders.cashier_id + items | 销售额 / 客单数 / 平均客单 / 退款率 |

#### 2.5.2 SQL Trigger 兜底审计

```sql
CREATE TRIGGER trg_products_ai AFTER UPDATE ON products
BEGIN
  INSERT INTO audit_logs_snapshot(
    table_name, row_id, op, old_json, new_json, ts
  )
  VALUES(
    'products', OLD.id, 'UPDATE',
    json_object('price', OLD.price, 'stock', OLD.stock, 'is_86', OLD.is_86),
    json_object('price', NEW.price, 'stock', NEW.stock, 'is_86', NEW.is_86),
    strftime('%s','now')
  );
END;
```

> 应用层 `audit_logs` 表补充 `actor_type/actor_id/reason`，Trigger 版本作灾备兜底并用于 diff 恢复。

#### 2.5.3 全离线容灾

```
启动时：
  LicenseVerifier → 缓存验证状态到 .cashier/license_cache.json
  {
    "verified_at": 1735689600,
    "expires_at": 1738368000,
    "payload_hash": "sha256",
    "signature_valid": true
  }

运行中（断网）：
  - 每日验签前检查缓存 → 有效期内直接信任
  - 离线期间订单/日结写入本地，标记 sync_status=offline_pending
  - 恢复联网 → cloud_push.retry_pending() 自动补推
  - 补推失败 → 队列告警（LED / 蜂鸣器可选）
```

#### 2.5.4 ESC/POS 指令层

- 驱动文件：`app/infra/hardware/drivers/escpos.py`
- 58mm / 80mm 热敏小票机：小票模板渲染 → ESC/POS 字节流 → USB/BT/串口发送
- 标签打印（茶饮/烘焙）：TSC 指令标签模板
- KDS 出餐屏：每行一个"待出" / "已出"状态 + 倒计时

#### 2.5.5 KDS 独立 WebSocket 服务

```
kds_server.py（独立进程）
  /ws/kds/{merchant_id}                  — 厨显端点
  POST /kds/internal/notify              — 内部 HTTP（POS 订单状态变更时调用）
  
POS 端订单状态变更 → call /kds/internal/notify → WS broadcast 到对应 KDS 屏幕
```

#### 2.5.6 验收标准

- 三张报表 CSV/PDF 导出
- 审计触发器验证
- 离线 72h 内正常收银；恢复后自动补推
- ESC/POS 测试报文正确
- +40 用例

---

### M6 · 集团统一下发 + 多门店调拨 + 分账

#### 2.6.1 云端下发 API（cloud/main.py）

| 端点 | 职责 |
|---|---|
| POST `/cloud/v1/policies/menu` | 总部下发菜单（覆盖 / 只更新差异） |
| POST `/cloud/v1/policies/prices` | 下发价格表（门店可 overrides） |
| POST `/cloud/v1/policies/members` | 下发会员策略 / 等级规则 |
| POST `/cloud/v1/policies/promotions` | 下发活动规则 |
| GET `/cloud/v1/policies/{tenant_id}/status` | 下发状态（已接收 / 已应用 / 冲突） |

#### 2.6.2 POS 接收端

- 定时轮询 `/cloud/v1/policies/.../status`
- 冲突处理：总部策略版本 > 本地版本 → 总部为准（门店可申诉）

#### 2.6.3 调拨与分账

| 表 | 职责 |
|---|---|
| `transfer_orders` | 调拨单（from_warehouse / to_warehouse / status） |
| `transfer_order_items` | 调拨明细 |
| `settlements` | 集团结算单（percent split / amount split） |

#### 2.6.4 License payload 扩展

```json
{
  "chain_push_scope": ["menu", "price", "member", "promotion"],
  "overridable_fields": ["price", "promotion"],
  "settlement_config": {
    "model": "percent",
    "hq_percent": 5,
    "store_percent": 95
  }
}
```

#### 2.6.5 验收标准

- 总部下发菜单 → 子店接收生效
- 调拨单创建/发货/收货完整链路
- 分账结算与云端账本一致
- +20 用例

---

## 3. 增量总工作量估算

| Milestone | 新增表 | 扩展列 | API | 前端页 | 用例 |
|---|---|---|---|---|---|
| M1 | 2 (tables + status_log) | 10 | 7 | 3 | +30 |
| M2 | 7 | 3 | 5 | 2 | +20 |
| M3 | 3 | 2 | 6 | 2 | +25 |
| M4 | 8 | 0 | 5 | 3 | +30 |
| M5 | 1 | 2 | 8 | 4 | +40 |
| M6 | 3 | 2 (license payload) | 5 | 2 | +20 |
| **合计** | **24** | **19** | **36** | **16** | **+165** |

预期总用例：105 (现有) + 165 = **270 cases**。

---

## 4. 数据迁移策略

采用 **migrations.py 自检升级** 模式：

1. 每周里程碑启动时，`bootstrap.py` 比对 `schema_version` 与当前期望版本
2. `ALTER TABLE ADD COLUMN` 补齐缺失列（老表不重建）
3. 新建表走 `CREATE TABLE IF NOT EXISTS`
4. 写入 `schema_migrations` 表记录执行日志，避免重复执行
5. 种子数据：`is_combo` 等列 default=0 兼容老数据；新会员等级批次初始化写入

```python
# 迁移检查示例
async def migrate_to_v1_1(session):
    cols = inspect(session.bind).get_columns("products")
    if not any(c["name"] == "is_combo" for c in cols):
        session.execute(text("ALTER TABLE products ADD COLUMN is_combo INTEGER DEFAULT 0"))
    # ...
    session.execute(
        text("INSERT INTO schema_migrations(version) VALUES ('v1.1')")
    )
    session.commit()
```

---

## 5. 架构守护原则

### 5.1 领域事件驱动

相关写操作统一走 `EventBus`，避免 Service 层散落业务逻辑：

```
OrderService.fire_merchant → emit OrderFired
  ├→ KDS Module      ：WS broadcast
  ├→ Audit Module    ：写入 audit_logs
  ├→ Inventory Module：菜品 BOM → 扣减原料
  └→ Member Module   ：记录消费标签
```

### 5.2 行业隔离

- `Tenant.industry` 启动时加载对应 `IndustryTemplate`
- API 入口校验该 feature 是否启用（`FeatureGuard.check(request, "recipe_bom")`）
- 前端按 feature_matrix 渲染菜单/视图

### 5.3 向后兼容

- 现有单店用户默认 `industry=fast_food`，feature_matrix 与 v1.0 一致（不新增可见功能）
- 仅当用户通过 License 升级并手动修改 Tenant.industry 时，新功能可见
- 旧 API 返回字段扩宽但不删除

---

## 6. 不在本范围（明确 exclusion）

- 移动端 App（iOS / Android）—— 微信小程序不在当前节奏
- 微信小程序扫码点餐（虽然行业模板定义为 future，但本阶段不做）
- 三方支付网关直连（通过 ESC/POS + 聚合支付屏跳转型）
- BI 大屏 / 数据仓库（至多报表 CSV 导出）
- 外卖平台 webhooks（仅做外卖业态标记，不做三方对接）

---

*方案设计：ZIQI + CatPaw · 2026-09-13*
*基于行业对标（银豹/客如云/客立满）+ v1.0 已上线基线生成。*
