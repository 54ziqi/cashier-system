# 柒号收银系统 APK 一键构建说明

> v3.3 · 2026-09-13 · 面向 Android 8.0+ (API 24+)

---

## 当前状态

✅ **已完成准备**：
- GitHub Actions 工作流 (`.github/workflows/build-apk.yml`)
- Android 打包工程配置修复（sourceSets、Maven 镜像、pip 镜像）
- APK 源码完整（Kotlin + Chaquopy Python 嵌入 → FastAPI 后端）
- JDK 17 本地安装完成

⚠️ **待用户操作**：
由于本地无法安装 Android SDK（Google 服务器在国内下载极慢），需要通过 GitHub Actions 在云端自动构建 APK

---

## 方案选择

### 方案 A：GitHub Actions 构建（推荐）

**优点**：零配置、免费、10-15 分钟出 APK、无需本地 Android SDK

**操作步骤**：

#### 第 1 步：创建 GitHub 仓库

1. 打开 https://github.com/new
2. 填写：
   - **Repository name**: `cashier-system`
   - **Visibility**: 选择 Private（私有，保护源代码）
3. **不要勾选** Add a README / .gitignore
4. 点击 **Create repository**

#### 第 2 步：上传项目代码

由于本地无法安装 Git（GitHub 下载极慢），使用 **Web 界面直接上传**：

1. 打开刚创建的仓库页面
2. 点击 **uploading an existing file**（或 drag files here）
3. 将项目根目录下所有文件和文件夹拖拽上传：
   ```
   app/
   cloud/
   config/
   static/
   ├── 收银系统完整设计与实现文档v3.2.md
   ├── 连锁云后台建设方案.md
   ├── 项目设计.md
   ├── requirements.txt
   └── run.py
   ```
4. 点击 **Commit changes**

#### 第 3 步：添加 Android 打包工程

1. 进入仓库页面，点击 **Add file → Create new file**
2. 文件路径：`.github/workflows/build-apk.yml`
3. 粘贴内容（已有现成内容，见下方详细工作流配置）
4. 提交
5. 继续创建 `打包/apk/` 目录，上传所有 APK 源码文件：
   - `打包/apk/build.gradle`
   - `打包/apk/settings.gradle`
   - `打包/apk/gradle.properties`
   - `打包/apk/gradlew`
   - `打包/apk/gradlew.bat`
   - `打包/apk/local.properties`  ← 留空或删除（云端自动配置）
   - `打包/apk/gradle/wrapper/...`
   - `打包/apk/app/build.gradle`
   - `打包/apk/app/src/...`

#### 第 4 步：触发构建

1. 所有文件上传完成后，工作流自动触发
2. 点击仓库的 **Actions** 标签，查看进度
3. 等待 10-15 分钟
4. 完成后，在 Artifacts 区域下载 APK

---

### 方案 B：本地构建（需完整 Android SDK）

如果你的电脑可以访问 Google 服务器，可以直接在本地构建：

```powershell
# 1. 确保 JDK 17 已安装（已完成）
java -version  # 应显示 17.0.20.1

# 2. 安装 Android SDK（含 API 34, Build Tools, NDK, CMake）
#    通过 Android Studio SDK Manager 安装

# 3. 构建
cd 打包\apk
.\gradlew.bat assembleDebug

# 4. APK 路径
dir app\build\outputs\apk\debug\
```

---

## 工作流 YAML 配置

创建文件 `.github/workflows/build-apk.yml`：

```yaml
name: Build Cashier APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        
      - name: Install SDK components
        run: |
          yes | sdkmanager --licenses
          sdkmanager "platforms;android-34" "build-tools;34.0.0" "ndk;25.2.9519653" "cmake;3.22.1"
          
      - name: Build debug APK
        run: |
          cd 打包/apk
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon --stacktrace
          
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: cashier-apk
          path: 打包/apk/app/build/outputs/apk/debug/*.apk
```

---

## APK 安装方法

### USB 传输到手机
1. 将 APK 文件传到手机（微信文件传输/数据线/邮件）
2. 在手机文件管理器中点击 APK 文件
3. 如提示"未知来源安装"，允许后安装

### 通过 ADB
```powershell
adb install -r 文件名.apk
```

---

## 启动应用

1. 点击"柒号收银"图标启动
2. 应用自动启动内嵌 Python 后端服务
3. WebView 加载本地 POS 界面
4. 30 天试用自动启用
5. 使用初始管理员密码登录（首次启动自动生成密码文件）

---

## 生成的 APK 参数

| 参数 | 值 |
|------|-----|
| 包名 | com.qihhao.pos |
| 最低系统 | Android 8.0 (API 24) |
| 目标系统 | Android 14 (API 34) |
| 支持架构 | arm64-v8a / x86_64 |
| 预估大小 | 80-120 MB |
| 签名 | Debug 签名（发布需正式 keystore） |

---

*文档版本: v1.0 · 2026-09-13*
