# 收银系统完整设计与实现文档 v3.2

> **用途**：本文档为 AI 实现指南，包含架构设计、代码骨架、部署方案。
> **目标系统**：轻量级离线收银系统，授权激活后完全离线稳定运行。
> **核心约束**：安装简单、部署简单、低配可用（512MB 内存）、零外部服务依赖。

---

## 目录

1. [项目概述](#一项目概述)
2. [设计原则](#二设计原则)
3. [系统架构](#三系统架构)
4. [运行分级模型](#四运行分级模型)
5. [离线授权与登录](#五离线授权与登录)
6. [Agent 旁路层](#六agent-旁路层)
7. [数据库设计](#七数据库设计)
8. [技术选型与依赖](#八技术选型与依赖)
9. [项目结构](#九项目结构)
10. [完整代码骨架](#十完整代码骨架)
11. [安装部署](#十一安装部署)
12. [API 接口](#十二api-接口)
13. [实现路线图](#十三实现路线图)
14. [验收清单](#十四验收清单)

---

## 一、项目概述

### 1.1 项目定位

参考亿客来收银系统的功能特性（双屏显示、称重收银、多硬件适配），设计一套基于 **Python 3.10+ / FastAPI / SQLite** 的轻量级收银系统，覆盖**商家端**与**管理员端**完整授权体系，支持扫码枪、小票打印机、电子秤、客显等硬件接入。

### 1.2 核心特性

| **特性** | **说明** |
|---------|---------|
| **完全离线** | 授权激活后无需网络即可完整运行（含登录、收银、打印） |
| **零外部依赖** | 无 Redis / Node / DB Server / Nginx，单进程运行 |
| **极低资源占用** | 空闲内存 < 80MB，启动 < 2s |
| **安装简易** | 一条命令安装，零配置启动 |
| **离线授权** | RSA 签名 License 本地验签，硬件指纹绑定 |
| **离线登录** | 本地 bcrypt 凭证 + HMAC Session Token |
| **Agent 旁路** | 推荐/异常检测/库存预警，异步低优先级 |
| **多级降级** | 任一组件故障不阻塞核心收银流程 |

---

## 五、离线授权与登录

### 5.1 离线 License 授权

**三层防护**：RSA-4096 非对称签名 + 硬件指纹绑定 + AES-GCM 本地加密。

**验证流程（完全离线）**：

```
启动时：
1. 读取 license.lic → 用内置公钥验签 → 失败进入受限模式
2. 解密 payload → 校验硬件指纹 → 不匹配拒绝
3. 校验 expire_at → 过期进入受限模式
4. 通过 → 加载 features → 启用对应功能

运行时（每 24h）：
5. 重新验签 → 防止运行时篡改
```

**受限模式定义**：

| **能力** | **正常模式** | **受限模式** |
|--------|----------|----------|
| 新建订单 | ✓ | ✗ |
| 查看历史订单 | ✓ | ✓ |
| 数据导出 | ✓ | ✓ |
| 会员操作 | ✓ | ✗ |
| 打印小票 | ✓ | ✗ |

### 5.2 离线登录

**Token 格式**：

```
{user_id}|{username}|{role}|{timestamp}|{hmac_signature}
```

---

## 七、数据库设计

### 7.2 核心表结构

```sql
-- 商户
CREATE TABLE merchants (
  id TEXT PRIMARY KEY, name TEXT NOT NULL, contact_name TEXT,
  phone TEXT UNIQUE NOT NULL, status TEXT NOT NULL DEFAULT 'active',
  license_type TEXT NOT NULL DEFAULT 'free', license_expire_at DATETIME,
  created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, version INTEGER NOT NULL DEFAULT 0
);

-- 本地凭证（离线登录）
CREATE TABLE local_credentials (
  id TEXT PRIMARY KEY, username TEXT UNIQUE NOT NULL, password_hash TEXT NOT NULL,
  role TEXT NOT NULL, is_active INTEGER NOT NULL DEFAULT 1,
  failed_attempts INTEGER NOT NULL DEFAULT 0, locked_until REAL NOT NULL DEFAULT 0,
  last_login REAL NOT NULL DEFAULT 0
);

-- 订单（幂等 + 乐观锁）
CREATE TABLE orders (
  id TEXT PRIMARY KEY, order_no TEXT UNIQUE NOT NULL, merchant_id TEXT NOT NULL,
  cashier_id TEXT, member_id TEXT, idempotency_key TEXT UNIQUE, status TEXT NOT NULL,
  total_amount INTEGER NOT NULL, discount_amount INTEGER NOT NULL DEFAULT 0,
  final_amount INTEGER NOT NULL, paid_amount INTEGER NOT NULL DEFAULT 0,
  change_amount INTEGER NOT NULL DEFAULT 0, sync_status TEXT NOT NULL DEFAULT 'pending',
  created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, version INTEGER NOT NULL DEFAULT 0
);

-- 订单项
CREATE TABLE order_items (
  id TEXT PRIMARY KEY, order_id TEXT NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
  product_id TEXT, product_name TEXT NOT NULL, barcode TEXT,
  unit_price INTEGER NOT NULL, quantity REAL NOT NULL DEFAULT 1,
  weight REAL DEFAULT 0, subtotal INTEGER NOT NULL, discount INTEGER NOT NULL DEFAULT 0
);

-- 支付
CREATE TABLE payments (
  id TEXT PRIMARY KEY, order_id TEXT NOT NULL REFERENCES orders(id),
  method TEXT NOT NULL, amount INTEGER NOT NULL, status TEXT NOT NULL,
  transaction_id TEXT, paid_at DATETIME, created_at DATETIME NOT NULL
);

-- 商品
CREATE TABLE products (
  id TEXT PRIMARY KEY, merchant_id TEXT NOT NULL, category_id TEXT,
  barcode TEXT, name TEXT NOT NULL, price INTEGER NOT NULL, cost_price INTEGER,
  stock REAL DEFAULT 0, unit TEXT DEFAULT 'pcs', is_weighing INTEGER DEFAULT 0,
  status TEXT DEFAULT 'active'
);

-- 会员
CREATE TABLE members (
  id TEXT PRIMARY KEY, merchant_id TEXT NOT NULL, card_no TEXT UNIQUE,
  name TEXT, phone TEXT, balance INTEGER DEFAULT 0, points INTEGER DEFAULT 0,
  level TEXT DEFAULT 'normal', created_at DATETIME NOT NULL
);

-- 设备
CREATE TABLE devices (
  id TEXT PRIMARY KEY, merchant_id TEXT NOT NULL, device_type TEXT NOT NULL,
  device_name TEXT NOT NULL, connection_type TEXT NOT NULL,
  config_json TEXT NOT NULL, status TEXT DEFAULT 'offline', last_heartbeat DATETIME
);

-- 审计日志
CREATE TABLE audit_logs (
  id TEXT PRIMARY KEY, actor_type TEXT NOT NULL, actor_id TEXT,
  action TEXT NOT NULL, resource_type TEXT, resource_id TEXT,
  before_json TEXT, after_json TEXT, created_at DATETIME NOT NULL
);

-- 同步队列（离线优先）
CREATE TABLE sync_queue (
  id TEXT PRIMARY KEY, entity_type TEXT NOT NULL, entity_id TEXT NOT NULL,
  operation TEXT NOT NULL, payload_json TEXT NOT NULL,
  sync_status TEXT NOT NULL DEFAULT 'pending', created_at DATETIME NOT NULL, synced_at DATETIME
);
```

---

## 十二、API 接口

### 12.1 商家端 API（/api/v1/merchant/*）

| 方法 | 路径 | 描述 | 权限 |
|------|------|------|------|
| POST | /auth/login | 收银员登录 | 公开 |
| POST | /auth/logout | 退出登录 | 收银员 |
| GET | /cashier/cart | 获取当前挂单列表 | 收银员 |
| POST | /cashier/cart/item | 扫码添加商品 | 收银员 |
| PUT | /cart/items/<id> | 修改数量 | 收银员 |
| DELETE | /cart/items/<id> | 移除商品 | 收银员 |
| POST | /cart/discount | 整体折扣 | 收银员 |
| POST | /cart/checkout | 结算 | 收银员 |
| POST | /cart/suspend | 挂单 | 收银员 |
| GET | /orders | 订单列表 | 收银员 |
| GET | /orders/<id> | 订单详情 | 收银员 |
| POST | /orders/<id>/refund | 申请退款 | 收银员 |
| GET | /products | 商品列表 | 收银员 |
| GET | /products/search | 扫码查商品 | 收银员 |
| GET | /members/phone | 手机号查会员 | 收银员 |
| POST | /members/recharge | 会员充值 | 收银员 |
| GET | /dashboard/today | 今日销售数据 | 收银员 |

### 12.2 管理员端 API（/api/v1/admin/*）

| 方法 | 路径 | 描述 | 权限 |
|------|------|------|------|
| POST | /auth/login | 管理员登录 | 公开 |
| POST | /merchants | 创建商户 | 超级管理员 |
| GET | /merchants | 商户列表 | 超级管理员 |
| PUT | /merchants/<id> | 编辑商户信息 | 超级管理员 |
| DELETE | /merchants/<id> | 停用商户 | 超级管理员 |
| POST | /licenses | 签发授权许可 | 超级管理员 |
| GET | /licenses | 授权列表 | 超级管理员 |
| PUT | /licenses/<id>/disable | 禁用授权 | 超级管理员 |
| GET | /system/stats | 平台统计数据 | 超级管理员 |
| PUT | /system/config | 系统参数配置 | 超级管理员 |
| GET | /devices | 设备管理 | 商户管理员 |

---

## 十三、实现路线图

### Phase 1 — 基础骨架（当前阶段）
- [x] 项目结构初始化
- [x] 配置加载 + CLI
- [x] SQLite + WAL + 单写队列
- [x] 离线 License 授权（RSA-PSS + AES-GCM）
- [x] 离线登录（bcrypt + HMAC Session）
- [x] 健康检查 API
- [x] 基础 UI（登录页 + POS 框架）

### Phase 2 — 核心交易 ✅
- [x] 商品管理 API（CRUD）
- [x] 订单/订单项持久化
- [x] 购物车状态机（CAS 并发安全）
- [x] 支付流程（现金/会员余额/在线支付）
- [x] 会员 API + CRM（余额/积分/等级）
- [x] 打印小票驱动对接（DEM 链路已通）

### Phase 3 — 硬件集成 ✅
- [x] 扫码枪实时监听（DEM scanner.py）
- [x] 小票打印机 USB/串口（DEM printer.py）
- [x] 电子秤协议适配（DEM scale.py）
- [x] 客显 WebSocket（DEM display.py）
- [x] 设备注册表 + DesktopBridge + 5 端点设备 API
- [x] L0-L4 设备识别体系

### Phase 4 — Agent + 报表 ✅
- [x] Agent 旁路（异常检测/库存预警/推荐）
- [x] 7 视图本地数据看板
- [x] 财务报表（损益/现金流/资产负债表）

### Phase 5 — 企业级 ✅
- [x] 多业态模板（快餐/正餐/茶饮/火锅/烘焙/零售）
- [x] 多角色权限（收银员/店长/总部/管理员）
- [x] 云端聚合 + 实时 WebSocket 推送
- [x] 三平台打包工程（Windows/macOS/Android）

---

## 十四、验收清单

### 14.1 L0 功能验收

| **编号** | **验收项** | **通过标准** |
|---------|----------|-----------|
| L0-01 | 首次启动 | 自动创建 admin 账户 + 30天试用 License |
| L0-02 | 离线登录 | 使用初始密码登录成功 |
| L0-03 | 商品 CRUD | 增删改查商品成功 |
| L0-04 | 扫码收银 | 扫描商品条码后自动添加购物车 |
| L0-05 | 现金结算 | 选择现金支付后订单状态变为 paid |
| L0-06 | 挂单/取单 | 挂单后购物车清空，取单后恢复 |
| L0-07 | 会员识别 | 输入手机号自动识别会员并显示余额 |
| L0-08 | 小票打印 | 结算后小票内容正确 |
| L0-09 | 离线运行 | 拔网后核心收银功能正常 |
| L0-10 | License 过期 | 过期后进入受限模式（禁止新建订单） |

### 14.2 性能验收

| **编号** | **指标** | **目标** |
|---------|---------|---------|
| P-01 | 冷启动时间 | < 2s |
| P-02 | 空闲内存 | < 80MB |
| P-03 | 单笔结账延迟 | P95 < 80ms |
| P-04 | 数据库 10万订单 | < 120MB |
